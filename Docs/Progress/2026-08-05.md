# 작업 로그 — 2026-08-05

## [00:10] 하네스 경량화 — 스모크 실측 체계 전환

### 완료된 작업
- `~/.claude/skills/harness/SKILL.md` — ① 단계 2에 "Critic 생략 조건(정형 작업)" 추가: 기존 파일 수정 2곳 이내·각 5줄 이내 + 같은 카테고리 성공 패턴 존재 시 에이전트 미실행, 메인 루프 인라인 체크 + 셀프 리뷰 블록("틀릴 수 있는 지점 3개")으로 대체 ② 단계 3 머지 규칙에 Critic 생략 시 처리 추가 ③ 단계 6 "반증 실측(적대 3개)" → "스모크 실측(플레이 모드 진입 최대 1회)" 교체, 채점 성향에서 반증 실측 의무 제거 ④ 성공 패턴 기록을 코드/시스템 작업으로 한정, "검증 기법" 섹션 신규 작성 중단 ⑤ 태그 통계 갱신 절차 전체 삭제 + 단계 7 "⚠ 태그 재검토 권장" 블록 삭제
- `.claude/harness-eval.md` — "적대 검증 (17점)" 섹션을 "스모크 실측 (17점)"으로 교체(배점 유지). 95점 조건·기능 충족 상한 50% 문구·MCP 실측 문구를 스모크 기준으로 연동 수정
- `CLAUDE.md` — 하네스 전달 규칙의 Critic(생략 조건)·Evaluator(스모크 실측) 항목 동기화
- `Docs/harness-logs/harness-success-patterns.md` — 태그 통계 블록 삭제, 기록 대상 한정 문구 추가
- `Docs/Progress/HANDOFF.md` — 확정 사양에 "하네스 경량화: 스모크 실측 체계" 결정 기록

### 결정 배경 (코드 변경 없는 합의 포함)
- 하네스 소요가 초기 ~10분 → 40분+로 비대해진 원인을 타임라인으로 실측: 점프대 하네스 43분 중 실측 20분+ / Planner+Critic 12분 / 코드 생성 11분. 규칙 누적의 전환점은 7/18 반증 채점 개편(`f0032a5`)
- 역할 분담 원칙 확정: **눈으로 판별 불가능한 것(틱 결정성·좌표 수치·직렬화 연결)만 하네스가 수치 실측**, 재미·손맛·회귀·구석 찌르기(적대 시나리오)는 사용자 플레이테스트가 담당
- Planner 풀 계획은 유지 — 델타 계획 축소안은 사용자가 기각 (계획 부실 → refine 재실행이 더 비쌈)
- 목표 소요: 정형 작업 ~15-20분 / 시스템급 ~25-30분
- SKILL.md는 전역 파일이라 LostPages 하네스에도 동일 적용됨

### 다음 단계
- Level_1_4~1_8 사용자 플레이테스트 (점프대 발사감 포함 — HANDOFF 다음 작업 1번)
- 다음 하네스 실행에서 경량화 체계 실제 소요 시간 검증

## [시간 미기록] 엔딩 연출 — ALL CLEAR + 정체성 봇 + 잔상 (하네스 98/100, 1회차)

### 완료된 작업
- `Assets/Scripts/Ending/EndingBot.cs` 신규 — 엔딩 전용 간이 AI. Init(IdentityData)로 틴트/이동속도/점프력 주입(정체성별 차등), FixedUpdate에서 수평 속도만 덮어쓰기(y 보존), 접지 시 랜덤 점프 + 랜덤 방향 전환, 전방 레이캐스트(Ground 256, useTriggers=false)로 벽 반전
- `Assets/Scripts/Ending/EndingSequence.cs` 신규 — 엔딩 총괄. Begin(정체성 복사본, levelParent): 엔딩 무대 + 봇 종류별 1기 스폰 + ALL CLEAR 하강 코루틴(SmoothStep) + UI 숨김(activeSelf 스냅숏) + 도크 Suppress. Stop(): 소유 리스트 역순 정리, 이중 호출 안전
- `Assets/Prefabs/Ending/EndingBot.prefab` — Player 동일 스프라이트/물리(mass 1, gravityScale 3), Default 레이어, AfterimageTrail 부착
- `Assets/Prefabs/Ending/Level_Ending.prefab` — 바닥(top y=-7.71, 폭 40.53) + 좌우 벽(x±20.3, top -1.0 — jumpForce 14 도달 상단 -3.38 차단)
- `LevelManager.cs` (185→261줄) — CleanupCurrentLevel() 추출(라인 이동만), 마지막 레벨 클리어 시 페이드 경유 StartEnding(), 엔딩 중 Enter/N 재시작(Stop→풀 Clear→LoadLevel(0)), 디버그 키 엔딩 정리 분기
- `AfterimageTrail.cs` (2곳) — RequireComponent에서 CharacterActor 제거, `_actor != null &&` 가드 — 봇에서 CharacterActor 없이 잔상 동작
- `CharacterSelectUI.cs` (2곳) — SetSuppressed(bool): 엔딩 중 State=Selecting 잔존으로 도크 재표시되는 문제 봉인
- SampleScene — Canvas 하위 AllClearLabel(legacy Text, Pretendard-Bold 120pt, 검정, anchor top-center, y +200→-300) + EndingSequence(=== MANAGERS ===) + 참조 연결

### 코드 설명
#### 컨테이너/자료구조 선택 이유
- EndingSequence._bots는 List<GameObject>(봇 수 = 해금 종류 수, 가변) — 소유 리스트 기반 파괴 규약(#3 패턴)
- _hiddenPreviousStates는 bool[](숨김 대상과 1:1, Begin 시점 크기 확정) — activeSelf 스냅숏 복원용
- EndingBot 질의 버퍼는 크기 1 고정 배열 + ContactFilter2D — 매 FixedUpdate 무할당

#### 함수 설명
- LevelManager.CleanupCurrentLevel: Teardown+캐릭터/레벨 파괴. LoadLevel과 StartEnding 공용
- LevelManager.StartEnding / RestartFromEnding: 엔딩 진입(_isEnding=true, Begin 호출) / 이탈(Stop→해금 풀 Clear→LoadLevel(0))
- EndingSequence.Begin/Stop: 엔딩 무대·봇·라벨 코루틴·UI 숨김을 한 쌍으로 세우고 걷는다
- CharacterSelectUI.SetSuppressed: RoundState 안 건드리고 도크만 봉인

#### 복잡한 코드 설명
- 엔딩은 RoundManager 상태 기계 밖 — Teardown 후 _isInitialized=false 휴면 가드가 Enter 녹화 충돌을 공짜로 차단. RoundManager 0줄 수정
- 엔딩 재시작 콜백은 Stop이 해금 풀 Clear보다 반드시 먼저(Begin이 복사본을 보관하지만 순서도 고정)
- AllClearLabel은 FadeOverlay보다 앞 sibling — 페이드에 덮이도록

### Evaluator 잔여 Minor
- EndingSequence.cs:148 도달 불가 방어 분기 / LevelManager.cs:225-231 엔딩 진입 페이드 중 PageDown 레이스(디버그 전용) / EndingBot.cs:78-80 주석 사실 오류 / 잔상 루트 이름 EndingBot(Clone)_Afterimages

### 다음 단계
- 사용자 플레이테스트: 마지막 레벨 클리어 → 엔딩 체감 (봇 움직임 신남 정도, ALL CLEAR 타이밍, 잔상 밀도 — 전부 Inspector 튜닝 가능)
- 포트폴리오 영상 촬영
