# 2026-08-17 작업 로그

## [17:42] 차단형 장애물 사선 빗금 셰이더 (룩 실험 1차)

### 완료된 작업
- `Assets/Art/Shaders/HatchedObstacle.shader` 신규 — CloneGhost 구조 재사용(SRP Batcher CBUFFER·Core2D.hlsl include·unity_SpriteColor 경로 동일), 수평 스캔라인 대신 45° 사선 빗금. 스크린 공간 픽셀 좌표 기준(카메라 이동에도 화면 고정 — 클론 규약과 동형). 지터 없음, 불투명(스프라이트 알파 그대로). 프로퍼티: `_HatchStrength`(0.3) / `_HatchPeriod`(8px) / `_HatchWidth`(3px) — 전부 Inspector 튜닝 가능
- `Assets/Art/Materials/HatchedObstacle.mat` 신규 (guid `133b3b364c95b3643a41ea19127527ac`)
- 머티리얼 적용 5곳: `Spikes.prefab`, `TimedDoor.prefab`(원본 프리팹 — Level_1_6/1_7 중첩 인스턴스는 자동 상속, 오버라이드 없음 확인), `Level_1_1/1_3/1_4.prefab` 내장 Door 3개
- 제외: FallZone(렌더러 없음), 스위치·발판·포탈·점프대(차단 장치 아님)

### 코드 설명
#### 복잡한 코드 설명
- **빗금 좌표 환산**: `(positionCS.x + positionCS.y) × 0.7071`로 45° 사선의 수직 거리를 얻고, period로 접어 스트라이프 중심 거리 d를 계산. `saturate(halfWidth − d + 0.5)`로 1px 소프트 에지(계단 방지)
- **가산항 × alpha**: 투명 픽셀 자리에 빗금만 떠 보이는 것 방지 — CloneGhost와 동일 이유
- **빗금은 가산(밝게)**: 장애물이 어두운 색(가시 빨강/문 갈색)이라 밝은 빗금이 잘 읽힘. 기존 색상(`m_Color`)은 셰이더가 그대로 곱해 유지

### 검증
- 셰이더/에셋 리프레시 후 콘솔 에러·워닝 0
- 플레이 모드 실측(진입 1회): Level_1_1 문 + Level_1_6 가시·TimedDoor 빗금 렌더 확인, hatched 렌더러 3기(1_6) 카운트 일치
- 스크린샷: `Assets/Screenshots/hatch-door-check.png`, `hatch-spikes-check.png`

### 다음 단계
- 사용자 룩 확인 (빗금 강도/간격/굵기 취향 — 머티리얼 Inspector에서 즉시 튜닝 가능)
- 채택 시: 반투명·경고색 변형 등 확장 판단 / 폐기 시: 머티리얼 참조 5곳 원복(`a97c1056…`)

## [17:48] 빗금 테두리 추가 (사용자 피드백 반영)

### 완료된 작업
- `HatchedObstacle.shader`에 `_BorderWidth`(4px, Range 0~24) 추가 — 스프라이트 가장자리를 원래 색 실선으로 남기고 그 안쪽에서만 빗금 렌더

### 코드 설명
#### 복잡한 코드 설명
- **테두리 폭 픽셀 환산**: UV 가장자리 거리(`min(uv, 1-uv)`)를 `fwidth(uv)`로 나눠 화면 픽셀 거리로 환산. UV 비율로 폭을 정하면 비균등 스케일(문 0.4×6)에서 세로/가로 테두리 두께가 달라지므로 픽셀 환산이 필수. `interior` 마스크를 빗금 가산항에 곱해 테두리 영역에서 빗금을 끔(1px 소프트 에지)

### 검증
- 콘솔 0, 플레이 실측: `hatch-border-door.png`(1_1 문 — 갈색 실선 테두리 + 내부 빗금), `hatch-border-spikes.png`(1_6 가시·TimedDoor 동일 구조)

### 다음 단계
- 사용자 룩 확인 (테두리 폭 4px 기본 — `_BorderWidth`로 즉시 튜닝 가능)

## [18:04] 캐릭터 SDF 절차적 룩 프로토타입 (그림판 탈출 실험 A안)

### 완료된 작업
- `Assets/Art/Shaders/CharacterBody.shader` 신규 — 스프라이트 대신 SDF(라운드 사각 거리 함수)로 몸통을 그림. 라운드 모서리 + 어두운 톤 외곽선(몸통 색 × 계수 — 정체성 4색 각각의 진한 버전 자동 생성) + 정적 요철(UV 공간 값 노이즈, `_Time` 미사용 — "삐뚤빼뚤하되 흔들리지 않는 선" 확정 사양 충족)
- `Assets/Art/Materials/CharacterBody.mat` 신규 (guid `c829872f750d3014184a97ecf3f94a20`) → `Player.prefab` 머티리얼 교체 (이전: 기본 스프라이트 mat `a97c1056…`)
- 프로퍼티 전부 Inspector 노출: `_CornerRadius`(0.10) / `_OutlineWidth`(3px) / `_OutlineDarken`(0.55) / `_WobbleAmp`(1.2px) / `_WobbleFreq`(12) / `_WobbleSeed`(0)

### 코드 설명
#### 복잡한 코드 설명
- **SDF 라운드 박스**: `length(max(|uv-0.5|-halfSize,0)) + min(max(p.x,p.y),0) - radius`. 음수=내부. 박스는 요철 진폭+AA 여유만큼 인셋해 쿼드 밖 잘림 방지
- **요철은 거리(d)에 노이즈 가산**: 경계를 밀고 당기는 방식이라 외곽선 안쪽 경계도 같이 울퉁불퉁해져 자연스러움. 진폭 1~2px 제한 = 시각 실루엣 ≈ 콜라이더(§4)
- **px 단위 프로퍼티는 fwidth 환산** (빗금 테두리와 동일 기법)

### 시스템 통합
- `CharacterActor`가 Awake에서 `sharedMaterial` 캐시 → Live/Idle 복원에 새 머티리얼이 자연 편입. 잔상(AfterimageTrail)도 sharedMaterial 복사라 동일 룩 승계
- **알려진 비대칭**: 클론은 CloneGhost(민짜 사각+스캔라인) 유지 — 채택 시 CloneGhost에 SDF 통합 필요(2차 작업)

### 검증
- 콘솔 에러 0. 플레이 실측: 라이브 캐릭터 mat=CharacterBody 확인, 스크린샷 `charbody-sdf-check.png`(supersize 2)
- 함정 기록: 프리팹 머티리얼 guid 교체가 **진행 중이던 플레이 세션에는 미반영**(사전 스폰 캐릭터가 옛 mat 유지) — 플레이 재시작으로 해결. 프리팹 YAML 수정은 플레이 밖에서 할 것

### 다음 단계
- 사용자 룩 확인 (요철 진폭/모서리 반지름/외곽선 두께·명도 — 전부 슬라이더 실시간 튜닝)
- 채택 시: CloneGhost에 SDF 통합(클론도 동일 실루엣) + EndingBot 적용 검토 / 이후 스쿼시·스트레치(1번 과제)

## [18:55] 라인 보일 미니 실험 (예비 카드 발동 — 정적 요철 어색함 대응)

### 완료된 작업
- `CharacterBody.shader`에 `_BoilRate`(Hz, 기본 3.5, **0 = 완전 정지 = 기존 확정 사양 복귀**) 추가 — 요철 노이즈 시드에 스텝화된 시간(`floor(_Time.y × rate)`)을 더해 요철 패턴을 초당 N회 통째로 교체

### 코드 설명
#### 복잡한 코드 설명
- **시드 스텝화(연속 시간 금지)**: 연속 `_Time`을 시드에 더하면 요철이 흐르는 물결처럼 이동한다. 스텝화하면 "그림을 다시 그린" 것처럼 패턴이 통째로 바뀜 — 이게 라인 보일. 클론 지터(24Hz 스텝화)와 같은 기법이라 프레임레이트 무관 동일 속도
- **배경**: 정적 요철이 "잘못 그려진 도형"으로 읽히는 어색함(사용자 피드백). 라인 보일은 2026-07-27 폐기됐으나 "예비 카드(추후 실험 가능)" 조항으로 실험. Baba 연상 리스크는 진폭 1.2px 소폭 유지로 완화

### 검증
- 콘솔 0. 플레이 실측: 두 시점 스크린샷(`charbody-boil-a/b.png`) 캐릭터 영역 픽셀 비교 → 경계부 249px 상이(내부 단색 동일) = 패턴 교체 실측 확인

### 다음 단계
- 사용자 체감 판단: 보일 채택(속도/진폭 튜닝) vs 정지 복귀(`_BoilRate 0`) vs 클린 라운드(`_WobbleAmp 0`)
- 채택 시 ART-DIRECTION.md 라인 보일 조항 갱신 필요(예비 카드 → 캐릭터 한정 채택)

## [20:23] 접지 부양 수정 (바닥 밀착 + 접지선 요철 고정)

### 완료된 작업
- `CharacterBody.shader` 수정 — ① SDF 박스 바닥을 `-_CornerRadius`까지 내려 접지선 밀착(4변 균일 인셋이 만들던 2~3px 부양 제거). 바닥이 -radius면 uv y=0 단면이 정확히 풀 폭이라 접지선은 각지게 닿고 라운딩은 그 위에서 시작 ② 바닥 근처(높이 하위 18%) 요철을 smoothstep으로 0 페이드 — 서 있는 라인이 보일마다 들썩이면 그 자체가 부양으로 읽힘

### 검증
- 콘솔 0. 픽셀 스캔 실측: 캐릭터 중앙 세로선에서 몸통(y≤1998)→지면(y≥1999) 직결, **흰 틈 0줄**. 스크린샷 `charbody-grounded-check.png`
- 부수 효과: 바닥 변에는 외곽선 밴드가 안 보임(박스 바닥이 쿼드 밖이라 클리핑) — 어두운 지면이 시각적으로 그 역할을 대신해 무해

### 다음 단계
- 사용자 룩 확인 (움직이는 보일 + 접지 체감)

## [20:29] 정사각 비율 복원 (위쪽 인셋 2배)

### 완료된 작업
- `CharacterBody.shader` 1줄 수정 — `hi.y`를 `1-inset`→`1-2·inset`. 좌우는 양쪽 인셋(폭 1−2·inset)인데 바닥 밀착 후 세로는 위쪽 1·inset만 깎여 세로가 ~3px 길어 보이던 문제(사용자 피드백). 이제 보이는 폭 == 보이는 높이
- 검증: 콘솔 0. 사용자 플레이 세션 진행 중이라(활성 캐릭터 = 클론 상태) 플레이 재실측은 생략 — 폭/높이 동일은 식으로 보장, 체감 확인은 사용자 몫. 셰이더 변경은 실행 중 세션에 즉시 반영됨

### 다음 단계
- 사용자 룩 확인 (정사각 비율 + 보일 + 접지 종합)

## [20:34] 몸통 크기 복원 (인셋 방식 → 쿼드 확장 방식)

### 완료된 작업
- `CharacterBody.shader` 구조 변경 — 몸통을 사방 ~3px 줄여 그리던 "안쪽 인셋" 방식 폐기. 버텍스에서 쿼드를 `1+2·_EdgeMargin`(기본 0.08)배 확장하고, 프래그먼트에서 UV를 콜라이더 기준(`uvC`)으로 역매핑 → **박스 경계 == 콜라이더 경계**(몸통 꽉 참), 요철·AA는 콜라이더 바깥 마진으로 삐져나감
- 이전 회차의 위쪽 인셋 2배 보정은 자연 소멸(인셋 자체가 사라짐). 바닥 -radius 밀착·하위 18% 요철 페이드는 uvC 기준으로 유지

### 코드 설명
#### 복잡한 코드 설명
- **uvC 역매핑**: `(uv-0.5)×expand+0.5` — 확장 쿼드 위에서 uvC 0..1이 정확히 콜라이더 경계. 이후 모든 계산(SDF/노이즈/페이드/px 환산)이 uvC 기준이라 기존 식 재사용
- **§4 정합 회복**: 인셋 방식은 실루엣 < 콜라이더(보이지 않는 마진에 부딪히는 느낌)였음. 쿼드 확장 방식은 실루엣 == 콜라이더, 요철만 ±1.2px 바깥 진동
- **제약**: `_WobbleAmp`를 마진 픽셀(0.08×47≈3.8px)−1 이상으로 올리면 요철이 쿼드 경계에 클리핑 — 진폭 크게 올릴 땐 `_EdgeMargin`도 함께

### 검증
- 콘솔 0. 픽셀 실측: 몸통 폭 96px ≈ 콜라이더 95px(47.37px/unit×2, 요철 오차 1px), 높이 동등(지면 AA 제외 시 ~97px). 스크린샷 `charbody-fullsize-check.png`

### 다음 단계
- 사용자 룩 확인 — 크기·비율·접지·보일 종합. 이걸로 A안 셰이더 골격은 안정

## [20:39] 정사각 재붕괴 수정 (콜라이더 바닥 클립)

### 완료된 작업
- `CharacterBody.shader` — `shape *= saturate(uvC.y/uvPerPx + 0.5)` 1줄 추가 (콜라이더 바닥 아래 1px AA 클립)

### 코드 설명
#### 복잡한 코드 설명
- **쿼드 확장의 숨은 부작용**: 접지선 평평화용 박스 바닥 연장(-radius)은 원래 쿼드 경계(=콜라이더 바닥)에서 자동으로 잘렸는데, 쿼드를 확장하자 연장분(≈4.7px)이 실제로 그려져 몸통이 지면에 파묻힌 모양 → 세로 99~100px vs 가로 93~95px 비정사각(사용자 피드백 → 픽셀 실측으로 원인 확정). 명시적 클립으로 이전 동작 복원

### 검증
- 콘솔 0. 재실측: 폭 91~95 / 높이 94~95px(요철 ±1.2px 오차 내 정사각), 바닥 y=1998 = 지면(1999) 밀착 유지. 스크린샷 `charbody-square-check.png`

### 다음 단계
- 사용자 룩 확인 — A안 셰이더 골격 안정(크기·정사각·접지·보일). 다음 결정: 보일 채택 여부 → 클론 통합/스쿼시·스트레치

## [20:42] 바닥 "잘림" 인상 해소 (4모서리 대칭 라운딩)

### 완료된 작업
- `CharacterBody.shader` — 바닥 특수 처리(박스 -radius 연장 + 콜라이더 바닥 클립) 전부 폐기 → 4모서리 대칭 라운드 박스로 단순화(`abs(uvC-0.5)-(0.5-radius)` 대칭식 복귀). 바닥 요철 페이드(하위 18%)만 유지

### 코드 설명
#### 복잡한 코드 설명
- **"잘림" 인상의 원인**: 위 모서리는 둥근데 바닥은 클립으로 자른 직각 + 바닥 변만 외곽선 부재 → 밑동 싹둑 인상(사용자 피드백). 대칭 라운딩으로 바닥에도 외곽선 밴드가 생기고 형태 언어가 통일됨
- **부양 회귀 아님**: 바닥 중앙 평평 구간(폭−2·radius ≈ 95−9=86px)이 지면에 그대로 닿음. 모서리 노치(≈4.7px)는 "둥근 캐릭터가 서 있는" 표준 실루엣(Draknek류)으로 읽힘 — 이전 부양 문제는 전폭 흰 틈+들썩임이었고 이번엔 둘 다 없음

### 검증
- 콘솔 0. 실측: 폭 94~96/높이 91~93(모서리 열은 곡률로 짧아지는 게 정상, 중앙 93 — 요철 오차 내 정사각), 바닥 중앙 y=1997~1998 지면 밀착. 스크린샷 `charbody-rounded-bottom.png` — 시각 확인 양호

### 다음 단계
- 사용자 룩 확인. 노치가 거슬리면 `_CornerRadius` 하향(0.10→0.06~0.08)으로 미세 조정

## [20:47] 바닥 보일 복원 (바깥 방향 전용 요철)

### 완료된 작업
- `CharacterBody.shader` — 바닥 근처 요철 "0 고정"을 "바깥쪽(아래) 방향만 허용"으로 교체: `wobble = lerp(min(wobble,0), wobble, smoothstep(0,0.18,uvC.y))`

### 코드 설명
#### 복잡한 코드 설명
- **모순 해소**: 바닥 안쪽 요철 = 몸통·지면 사이 흰 틈 = 부양 들썩임(이전 피드백) ↔ 요철 0 = 바닥만 보일 정지 비일관(이번 피드백). 바깥 요철만 허용하면 접지 중엔 지면에 겹쳐 들어가 무해(캐릭터가 지면 위에 그려짐), 공중에선 정상 보일
- wobble 부호 규약: 양수 = sd 증가 = 안쪽 수축 / 음수 = 바깥 팽창 → `min(wobble, 0)` = 바깥 전용

### 검증
- 콘솔 0. 바닥 밴드(y1994~2004) 두 시점 비교: diff 50px = 바닥 보일 가동, 흰 픽셀 28개 = 모서리 노치 영역(설계상)뿐 — 중앙 평평 구간 흰 틈 0

### 다음 단계
- 사용자 룩 확인 — 4변 보일 일관성 + 접지 안정 종합

## [20:52] [임시] 녹화 시간 제한 비활성화 (사용자 요청)

### 완료된 작업
- SampleScene `RoundManager._maxRecordSeconds` **15 → 999** (씬 직렬화 값만, 코드 0줄) — 룩 테스트 중 15초 자동 확정 차단. HUD "남은 시간"은 999부터 카운트다운 표시됨(기능 자체는 살아 있음)

### 다음 단계
- **⚠ 테스트 끝나면 15으로 원복 필수** — 15초 상한은 레벨 설계·리플레이 HUD 카운트다운(상한 15s 기준)의 전제값

## [20:53] 클론에 SDF 몸통 통합 (CloneGhost 개편)

### 완료된 작업
- `CloneGhost.shader` 개편 — CharacterBody의 SDF 몸통 문법(대칭 라운드 박스 + 어두운 톤 외곽선 + 보일 요철 + 바닥 바깥 전용 + 쿼드 확장/uvC 역매핑) 통합. 클론 고유 연출은 유지: 스캔라인(스크린 공간)·밝기 지터(24Hz 스텝)·CloneAlpha 규약(알파 = shape × tint.a — 리빌 은폐 상태에서 스캔라인 사각형 안 뜨는 기존 계약 보존)
- 프로퍼티 8종 추가(CharacterBody와 동일 기본값) — CloneGhost.mat은 기존 오버라이드(스캔라인/지터)만 저장돼 있어 신규 프로퍼티는 셰이더 기본값 자동 적용
- 쿼드 확장은 위치 이동이 아닌 대칭 마진이라 "고스트 위치 변형 금지" 규약(궤적=판정 근거)과 무관 — 주석 명시

### 검증
- 콘솔 0. 플레이 실측: 테이크1 확정 → 클론 재생 상태에서 mat=CloneGhost 확인, 스크린샷 `clone-sdf-check.png` — 라운드 몸통+스캔라인 동시 확인
- 알려진 Minor: 전 클론이 sharedMaterial 공유라 요철 패턴·보일 위상이 동일(개별화하려면 MaterialPropertyBlock 필요 — 화면상 클론들이 떨어져 있어 체감 낮음, 필요 시 후속)

### 다음 단계
- 사용자 룩 확인 (클론 다수 배치 시 보일 동기화 체감 포함)
- 잔여: EndingBot 룩(별도 프리팹 — 미적용), 박스/기믹 라운드+외곽선 확장(보일 0) 판단

## [21:02] 보일 전면 적용 (환경·오브젝트 전체 — 사용자 결정)

### 완료된 작업
- 사용자 결정: "Baba와는 아예 다른 게임이라 전체 보일 문제없다" — 3계층 규칙 중 "지형=고정" 폐기, 전면 보일 실험
- `EnvironmentBoil.shader` 신규 — 색·형태는 그대로 두고 **경계만 안쪽 방향 전용으로 갉아먹는 보일**(실루엣이 콜라이더 밖으로 절대 안 커짐 → 판정 오해 없음). 노이즈 좌표를 `GetObjectToWorldMatrix` 스케일로 보정해 비균등 스케일(바닥 40×1.7)에서도 유닛당 요철 밀도 균일(캐릭터와 동일 질감), 오브젝트 부착이라 이동 발판/박스도 요철이 몸에 붙어 이동
- `HatchedObstacle.shader`에 동일 경계 보일 추가(가시·문) — 테두리 판정도 흔들린 거리(edgePxW)를 공유해 테두리 밴드가 경계 따라 출렁임
- `EnvironmentBoil.mat` 생성(guid `214dcfb8…`) → **프리팹 15개 39곳 일괄 교체**(PowerShell guid 치환, UTF-8 no BOM 유지): Level_1_1~1_8·Level_Ending(지형/출구/압력판/스폰 표식), 기믹 5종(Crumbling/Portal/JumpPad/MovingPlatform/ToggleSwitch), PushableBox
- **제외**: Settings 2개(Renderer2D·URP globals — 기본 머티리얼 참조, 금지), EndingBot(행위자 — 히어로 룩 적용은 별도 결정)

### 검증
- 콘솔 0. 지형 상단 경계 밴드(전폭) 두 시점 비교 diff 1402px = 지형 보일 가동. Level_1_6 스크린샷(`envboil-gimmicks.png`)에서 지형·이동발판·가시·문·출구 전부 경계 요철 확인
- 원복 경로: guid `214dcfb8…`→`a97c1056…` 역치환이면 15파일 일괄 복구

### 다음 단계
- 사용자 종합 룩 확인 (전면 보일 체감 — 과하면 개별 `_BoilRate 0`/진폭 조정, 또는 지형만 원복)

## [21:10] 오브젝트 테두리 (검은 땅 제외 — 셰이더 자동 판별)

### 완료된 작업
- `EnvironmentBoil.shader`에 `_OutlineWidth`(3px)/`_OutlineDarken`(0.55) 추가 — 외곽선 = 몸통 색 × 어두운 계수(캐릭터와 동일 문법). 테두리 판정은 흔들린 경계 거리(edgePxW) 공유 → 보일 따라 출렁임

### 코드 설명
#### 복잡한 코드 설명
- **"검은 땅 제외"를 에셋 분리 없이 달성**: 검정 × 0.55 = 검정이라 지형에선 외곽선이 물리적으로 안 보임. 색 있는 오브젝트(박스·발판·스위치·출구·압력판 등)만 테두리가 드러남 → 프리팹 0개 수정, 머티리얼 1개 공유 유지
- **주의(잠복)**: 지형을 검정이 아닌 색으로 바꾸는 날엔 지형에도 테두리가 나타남 — 그때는 지형 전용 머티리얼 분리(EnvironmentBoil.mat 복제, `_OutlineWidth 0`) 필요

### 검증
- 콘솔 0. Level_1_4 실측(`envboil-outline-check.png`): 박스 진한 주황 테두리/출구 진한 초록/압력판 진한 회색, 검은 벽·바닥 무변화

### 다음 단계
- 사용자 종합 룩 확인 — 룩 방향 확정 시 커밋 권장(셰이더 4종+머티리얼 3종+프리팹 21개 미커밋 누적)

## [21:14] 보일·테두리 파라미터 전 머티리얼 통일 (캐릭터 기준)

### 완료된 작업
- 캐릭터 머티리얼의 사용자 튜닝 값(**_BoilRate 7Hz, _OutlineWidth 5px** — 기본 3.5/3에서 상향)을 기준으로 통일:
  - `EnvironmentBoil.mat`: BoilRate 3.5→7, OutlineWidth 5 명시
  - `CloneGhost.mat`: BoilRate 7, OutlineWidth 5 추가(기본값 사용 중이었음)
  - `HatchedObstacle.mat`: BoilRate 7 추가, **BorderWidth 1→5**(사용자가 1로 낮췄던 값을 통일 지시에 따라 캐릭터 기준으로)
- 요철 진폭 1.2/밀도 12/명도 0.55는 전부 기본값 동일이라 무변경. 콘솔 0

### 다음 단계
- 사용자 체감 확인 — 빗금 테두리 5px가 두꺼우면 개별 조정 여지(통일 원칙 내 예외 판단)

## [21:19] 오브젝트 코너 라운딩 (EnvironmentBoil·HatchedObstacle)

### 완료된 작업
- 두 셰이더에 `_CornerRadius`(화면 px, 기본 4.7 = 캐릭터 실효 반지름 0.1UV×47px와 동일) 추가 — 모서리 영역(두 변 모두 반지름 이내)에서 경계 거리를 호(arc) 거리로 대체. 알파·외곽선·빗금 테두리가 같은 거리(edgePx)를 쓰므로 전부 라운딩을 따라감

### 코드 설명
#### 복잡한 코드 설명
- **픽셀 단위 반지름**: 오브젝트 크기가 제각각(압력판 0.3u~바닥 40u)이라 UV 비율 반지름은 부적합 — 화면 px 고정이 균일한 인상
- **거리 캡**: 반지름보다 깊은 내부에선 거리가 반지름으로 캡되지만 외곽선 판정(5px) 범위 밖이라 무해. 반지름 0일 땐 캡 공식이 전체를 지우므로 `>0.01` 분기 가드 필수

### 검증
- 콘솔 0. Level_1_4 실측(`envboil-corner-check.png`): 박스·출구·압력판·문·벽 끝단 라운딩 확인, 지형 슬라브 모서리도 살짝 둥글어짐(공유 머티리얼 — 손그림 문법과 일관)

### 다음 단계
- 사용자 종합 룩 확인 → 확정 시 커밋

## [21:30] 오브젝트 보일 세기 보정 (이동폭 절반 문제)

### 완료된 작업
- 사용자 "오브젝트 보일이 캐릭터보다 약하다" → 수치 대조로 원인 확정: 진폭 슬라이더는 동일(1.2)하나 **캐릭터는 양방향(±1.2 = 이동폭 2.4px), 오브젝트는 안쪽 전용(0~−1.2 = 이동폭 1.2px)** — 구조 차이로 체감 절반
- `EnvironmentBoil.mat`·`HatchedObstacle.mat` `_WobbleAmp` 1.2→**2.4** (이동폭 2.4px로 캐릭터와 동등, 안쪽 전용 유지 — 실루엣≤콜라이더 안전성 불변)
- 잠재 부작용 관찰 대상: 바닥 위 접촉면(캐릭터 발밑·박스 밑)에서 지형 상단이 최대 2.4px 파일 때 간헐 흰 틈 플리커 — 거슬리면 양방향+중앙 인셋 방식 재설계 필요

### 다음 단계
- 사용자 체감 재확인

## [21:37] 오브젝트 보일 양방향 전환 (잘린 느낌 해소 — 21:30 진폭 보정 대체)

### 완료된 작업
- 사용자 "오브젝트 보일이 영역 밖으로 못 나가 잘린 느낌" — 안쪽 전용 구조의 본질적 한계 확인 → **양방향 보일로 전환** (21:30의 진폭 2.4 보정은 미봉책이었으므로 1.2로 원복)
- `EnvironmentBoil`·`HatchedObstacle` 셰이더: **픽셀 기반 쿼드 확장** — 직교 투영의 px/유닛(`0.5×_ScreenParams.y×P._m11`)으로 (amp+2)px 상당의 월드 마진을 계산해 버텍스를 바깥으로 밀고, 프래그먼트에서 uvC 역매핑(0..1=콜라이더). 요철이 콜라이더 밖 마진까지 ±1.2px 부풀어 캐릭터와 동일 구조
- 캐릭터의 비율 마진(_EdgeMargin 8%) 방식은 지형 40유닛에서 수 유닛이 되므로 픽셀 기반이 필수. 노이즈 밀도는 uvC×sizeWS(월드 크기 varying)로 유닛당 고정 유지

### 코드 설명
#### 복잡한 코드 설명
- **px/유닛을 버텍스에서 환산**: 마진은 화면 픽셀 기준이어야 오브젝트 크기와 무관하게 균일한데, 버텍스 셰이더는 픽셀을 모름 → 직교 투영 행렬 P._m11(=1/orthoSize)과 화면 높이로 환산. 원근 카메라면 부정확하지만 이 게임은 직교 고정
- **실루엣 > 콜라이더 최대 1.2px 허용** — 캐릭터가 이미 쓰는 규약과 동일, 시각 오차 무시 가능. 접촉면(발밑) 흰 틈 플리커도 바깥 요철이 채워 21:30의 관찰 대상 해소

### 검증
- 콘솔 0. 사용자 플레이 세션에 핫 리로드 실측(`envboil-bidir-check.png`): 지형 상단·출구·문 경계가 밖으로도 부풂, 잘린 인상 해소

### 다음 단계
- 사용자 체감 재확인 → 확정 시 커밋

## [21:45] 셰이더 4종 옵션 체계 통일 (명칭·순서·단위·로직 — 사용자 요청)

### 완료된 작업
- **공통 옵션 블록 확립** (4종 동일 이름·순서·단위·기본값): `_OutlineWidth`(5px)/`_OutlineDarken`(0.55)/`_CornerRadius`(4.7px)/`_WobbleAmp`(1.2px)/`_WobbleFreq`(12/유닛)/`_WobbleSeed`(0)/`_BoilRate`(7Hz). 셰이더별 전용 옵션(클론 스캔라인·지터, 빗금 3종)은 공통 블록 뒤에 배치
- `CharacterBody.shader`·`CloneGhost.shader` **몸통 로직을 EnvironmentBoil과 동일한 픽셀 기반으로 재작성**: UV 비율 단위(_CornerRadius 0.1UV, _EdgeMargin 8%) 폐기 → px 반지름 + px 기반 쿼드 확장 + uvC×sizeWS 노이즈. 캐릭터 전용 접지선 보호(바닥 18% 바깥 전용 요철 — 새 거리 규약에선 `max(wobble,0)`로 부호 반전 주의)는 캐릭터·클론에만 유지
- `HatchedObstacle` `_BorderWidth`→`_OutlineWidth` 개명 + `_OutlineDarken` 추가(머티리얼 1.0 = 원색 테두리 룩 보존, 슬라이더 로직은 공통)
- 머티리얼 이관: CharacterBody.mat radius 0.1→4.7(단위 변환)·_EdgeMargin 제거 / HatchedObstacle.mat 키 개명+darken 1

### 검증
- 콘솔 0. 플레이 실측(`unified-params-check.png`): 캐릭터·클론 겸 전 오브젝트 룩 통일 유지, 캐릭터 이전 룩과 시각 동일(4.7px ≈ 0.1UV×47px)

### 다음 단계
- 이제 어느 머티리얼에서든 같은 슬라이더가 같은 방식으로 동작 — 룩 확정 시 커밋

## [21:53] 요철 파장 완만화 (미리보기 느낌 재현 — WobbleFreq 12→4)

### 완료된 작업
- 사용자 "인게임은 높고 좁게 솟는데 미리보기는 완만한 언덕 — 미리보기가 원하는 느낌": 원인 = 밀도 12/유닛이 인게임 47px/유닛에선 굴곡 폭 ~4px(진폭 1.2px 대비 급경사), 미리보기는 쿼드가 200px+로 그려져 굴곡 ~17px(완만). **4종 머티리얼 `_WobbleFreq` 12→4** (인게임 굴곡 ~12px = 미리보기 비율 근사)

### 검증
- 콘솔 0. 실측 `wobble-freq4-check.png`: 지형·캐릭터·출구 경계가 완만한 굽이로 전환

### 다음 단계
- 사용자 체감 확인 (더 완만하게 = freq 3 / 언덕 높이 = amp 상향) → 룩 확정 시 커밋

## [21:56] [임시] 지형 보일 OFF (A/B 비교용 지형 전용 머티리얼 분리)

### 완료된 작업
- `EnvironmentBoilStatic.mat` 신규(guid `ae6fa55d…`, EnvironmentBoil 셰이더 — `_WobbleAmp 0` = 요철·보일 정지, 나머지 값 동일) — 21:10에 예고했던 "지형 전용 mat 분리"가 이 시점에 현실화
- 레벨 프리팹 9개(1_1~1_8+Ending)에서 **색이 순수 검정(0,0,0)인 렌더러 20개**만 스크립트로 선별 교체(EnvironmentBoil guid → Static guid, 색 판별은 m_Materials 줄 이후 같은 블록 내 m_Color 검사). 검정 전수 17+3과 일치
- 오브젝트(색 있는 것)·기믹 프리팹은 EnvironmentBoil 유지 — 보일 계속

### 검증
- 콘솔 0. 실측(`terrain-static-check.png`): 지형 경계 직선·정지, 캐릭터·압력판·문·출구 보일 유지

### 다음 단계
- 사용자 A/B 판단: ① 지형 정지가 낫다 → 이대로 확정(3계층 규칙 부활) ② 지형도 보일 → guid 역치환(ae6fa55d→214dcfb8) 또는 Static.mat amp만 1.2로. 어느 쪽이든 1분 원복

## [22:00] 지형 독립 조절판 확정 (Static → TerrainBoil 개명 + 흔들림 복원)

### 완료된 작업
- 사용자 A/B 결론: "지형도 흔들리는 게 낫지만 별도 조절 필요" → `EnvironmentBoilStatic.mat`을 **`TerrainBoil.mat`으로 개명**(파일+메타 rename — guid `ae6fa55d…` 보존이라 지형 렌더러 20개 참조 그대로 유효) + `_WobbleAmp` 0→1.2(흔들림 복원, 공통값에서 시작)
- **셰이더 포크는 하지 않음**(사용자에게 설명) — 조절 값은 머티리얼 단위 저장이라 전용 머티리얼로 독립 튜닝 충분. 셰이더 복제는 4종 통일을 5종 동기화 부담으로 만드는 중복. 지형만 다른 "로직"이 필요해지는 시점에 포크
- 최종 머티리얼 구성: CharacterBody(캐릭터)/CloneGhost(클론)/EnvironmentBoil(오브젝트·기믹)/**TerrainBoil(지형 — 독립 슬라이더)**/HatchedObstacle(위험물)

### 검증
- 콘솔 0. 에셋 검증: TerrainBoil 로드 정상, amp 1.2, Level_1_1 지형 참조 1개 확인(개명 후 참조 유효)

### 다음 단계
- 사용자가 TerrainBoil.mat 슬라이더로 지형만 따로 튜닝 → 값 확정 시 커밋

## [22:06] 인스턴스별 시드 (동종 오브젝트 패턴 분화 — 4종 공통)

### 완료된 작업
- 4종 셰이더에 `instSeed` varying 추가 — 버텍스에서 **월드 위치를 0.5유닛 격자로 양자화해 해시**(`Hash21(floor(pos×2))×97`), 프래그먼트 boilSeed에 가산. 스크립트·프리팹·머티리얼 수정 0, MaterialPropertyBlock 불사용(SRP 배칭 유지)

### 코드 설명
#### 복잡한 코드 설명
- **왜 위치 해시인가**: 머티리얼 공유를 유지하면서 인스턴스를 구별할 유일한 셰이더 내 정보가 오브젝트 행렬. MPB 방식은 스크립트 부착+배칭 파괴라 기각
- **0.5유닛 양자화**: 쌍문(0.6유닛 간격)도 다른 격자에 떨어지게 하는 해상도. 이동체는 격자 경계에서 패턴 리롤 — 7Hz 보일 리듬에 마스킹되어 체감 없음. ⚠ `_BoilRate 0`(정지 모드)으로 쓰면 이동체가 0.5유닛마다 패턴 팝 — 정지 모드 채택 시 유의

### 검증
- 콘솔 0. Level_1_6 실측(`instance-seed-check.png`): 쌍문 두 문짝 경계 패턴 상이, 지형·발판·가시 각자 패턴

### 다음 단계
- 사용자 종합 확인 → 커밋 (오늘 룩 개편 전체)

## [22:12] UI 폰트 전면 Bazzi 교체

### 완료된 작업
- SampleScene의 UI Text 15개 폰트를 Pretendard-Bold → **Bazzi.ttf**(`Assets/Fonts/` — 사용자 반입, TTF판 사용)로 교체. 에디터 내 `execute_code`(SerializedObject 경유 아닌 .font 대입 + SetDirty + SaveScene)로 처리 — 씬 파일 직접 수정 대신 에디터 경유(열린 씬 충돌 방지). 프리팹·내장 폰트 사용처 없음(전수 grep)
- 한글 글리프 확인: Bazzi TTF/OTF 모두 한글 포함(`HasCharacter` 실측)

### 함정 기록 (오진 방지)
- **supersize 2 스크린샷이 Screen Space Camera UI를 왜곡/누락** — 첫 검증에서 "타이틀 거대화+HUD 실종"으로 보였으나 폰트 문제가 아닌 캡처 아티팩트(오늘 이전 supersize 2 캡처들도 HUD 부재였음). supersize 1 재실측으로 정상 확인. 메모리 unity-mcp-quirks #6에 등재. **UI 검증 캡처는 supersize 1 필수**

### 검증
- 콘솔 0. supersize 1 실측(`font-bazzi-title-ss1.png`/`font-bazzi-hud-ss1.png`): 타이틀 AFTER YOU/START + HUD 한글 3종 전부 Bazzi 정상 렌더

### 다음 단계
- 사용자 폰트 체감 확인 (Bazzi 라운드 룩 — 손그림 보일 문법과 톤 일치) → 커밋

## [22:22] UI 텍스트 라인 보일 (TextBoil 컴포넌트)

### 완료된 작업
- `Assets/Scripts/UI/TextBoil.cs` 신규 — `BaseMeshEffect` 상속(UI 표준 Shadow/Outline 계열). 글리프 정점을 틱마다 ±1.2px 독립 오프셋(네 모서리 따로 → 글자가 살짝 비뚤어지는 "다시 그린 글자" 인상), 7Hz 스텝(`floor(unscaledTime×rate)` — 셰이더와 동일 기법), 오브젝트별 랜덤 시드(동기화 방지, UI 전용이라 결정성 무관). `_amplitude`/`_boilRate` Inspector 노출, 0이면 정지
- 씬 Text 15개 전부에 부착(에디터 경유 + 씬 저장)

### 코드 설명
#### 컨테이너/자료구조 선택 이유
- **셰이더가 아닌 메시 이펙트인 이유**: 폰트는 아틀라스 텍스처라 UV 왜곡 셰이더는 인접 글리프 번짐 위험. 정점 변형은 번짐 0 + 기존 UI 머티리얼 그대로
#### 함수 설명
- `Update`: 틱 변화 시에만 `SetVerticesDirty()` → 메시 재생성 유도(누적 드리프트 없음 — 매번 원본 정점에서 다시 흔듦)
- `ModifyMesh`: 재생성 시 현재 틱 패턴으로 정점 오프셋
- `Hash`: 셰이더 Hash21과 같은 sin 의사난수([-1,1])

### 검증
- 콘솔 0. 타이틀 두 시점 캡처(ss1) 텍스트 영역 diff 1029px = 보일 가동, 정지 프레임은 자연스러운 손글씨(`textboil-a/b.png`)

### 다음 단계
- 사용자 체감 확인 (성능 우려 없음 — 15개 텍스트 7Hz 메시 재생성은 미미) → 커밋

## [22:28] 텍스트 보일 방식 교체: 정점 떨림 → 픽셀 획 보일 (사용자 피드백)

### 완료된 작업
- 사용자 "글자가 통째로 흔들리는 느낌, 획의 라인 보일이 아님" — 정점 방식(글자당 4정점)의 구조적 한계 → **폐기**: TextBoil 컴포넌트 15개 제거 + `TextBoil.cs` 삭제(씬 저장 후 삭제 — 미싱 스크립트 방지)
- `UITextBoil.shader` 신규 — UI/Default 기반(스텐실·ClipRect·AlphaClip 보일러플레이트 유지), 프래그먼트에서 **스크린 공간 노이즈로 폰트 아틀라스 샘플 UV를 픽셀 단위 왜곡** → 획 윤곽이 우글거리는 진짜 보일. 파라미터는 월드와 동일 의미(±1.2px/파장 12px/7Hz/시드 스텝화)
- `UITextBoil.mat` 생성 → Text 15개 material 할당(씬 저장)
- **제약**: 진폭을 크게 올리면 아틀라스 인접 글리프가 번져 들어옴(동적 폰트 패딩 한계) — ±1.2px에선 번짐 미관측. 크게 흔들고 싶어지면 TMP(SDF 아틀라스, 패딩 큼) 이관이 정공법(셰이더 주석에 명시)

### 검증
- 콘솔 0. 타이틀 영역 두 시점 diff 1029px = 보일 가동, 정지 프레임에서 획 가장자리 요철 확인·글자 위치 고정·번짐 0 (`uitextboil-a/b.png`)

### 다음 단계
- 사용자 체감 확인 → 오늘 룩 개편 전체 커밋

## [22:38] UI 이미지 보일 (버튼·밑줄·카드 도크 — UIBoil 셰이더)

### 완료된 작업
- `UIBoil.shader` 신규 — UI/Default 기반(스텐실·ClipRect 유지). 버텍스에서 쿼드를 (amp+1.5)px 확장(단순 Image의 UV 부호로 모서리 방향 판별 — 렉트 크기 정보 불요), 프래그먼트에서 원래 렉트 경계 기준 **양방향 보일 ±1.2px + 코너 라운딩 4.7px + 어두운 톤 테두리** — 월드 셰이더와 동일 문법. 노이즈는 스크린 공간(파장 12px) — UI는 화면 고정이라 자연스럽고 요소별 위치 차이로 패턴 자동 분화
- `UIBoil.mat` 생성 → Image 11개 할당: StartButton/NextButton/CardTemplate/Inner/Icon/Underline×3/TopLine/SideLineL·R. **제외**: FadeOverlay·TitlePanel(풀스크린 — 경계 화면 밖 + 페이드 안전), Dot(원형 스프라이트 REC 표시등)
- **제약 주석**: Sliced/Filled 타입 Image 비대응(UV 규약 상이 — 현 씬 전부 Simple), CanvasScaler 1920×1080 1:1 전제(마진 캔버스 유닛≈px 근사)

### 검증
- 콘솔 0. supersize 1 실측(`uiboil-title-a.png`/`uiboil-hud.png`): START 버튼 테두리 요철+라운딩, HUD 밑줄 손그림 선(2px 세선은 요철에 군데군데 끊겨 "손으로 그은 대시" 인상 — 스타일 일관, 과하면 amp 하향), 카드 도크 프레임·아이콘 보일 확인

### 다음 단계
- 사용자 체감 확인 (밑줄 대시화 취향 포함) → 오늘 룩 개편 전체 커밋

## [23:10] 배경 빗금 (BackgroundHatch — 손그림 노트 질감)

### 완료된 작업
- `BackgroundHatch.shader` 신규 — 흰 배경 위 **감산 계열** 45° 사선(장애물 빗금은 가산이라 흰 배경에서 비가시 → 분리). 선은 저주파 노이즈(_WaveAmp 1.5px/_WaveLength 24px)로 구불거리고 시드 스텝화 7Hz 보일 — 전체 언어와 동일 리듬. `_HatchDarken`(0.94)/`_HatchPeriod`(16px)/`_HatchWidth`(3px) 슬라이더
- `BackgroundHatch.mat` + 씬 오브젝트 `BackgroundHatch`(스프라이트 쿼드 41.5×23.5 @ (0,2), sortingOrder -100 — 구 실루엣 관례, 콜라이더 없음) 생성·씬 저장

### 함정 기록 (오진 방지 2건)
- **타이틀 화면에선 배경이 안 보임** — TitlePanel(흰 풀스크린 UI)이 월드 전체를 덮는 구조라 정상. 배경 확인은 반드시 인게임에서 (첫 실측 때 "렌더 안 됨"으로 오진 → 빨강+sortingOrder 500 테스트로 분리 확인)
- 머티리얼 .mat 직접 수정 후 즉시 플레이 시 **핫 리로드 지연 가능** — 값 반영 확인은 한 프레임 이상 지난 캡처로

### 검증
- 인게임 실측(`bg-hatch-ingame.png`): 배경 전체 사선 렌더(선 ~240 vs 바탕 253), 지형·오브젝트·HUD 가독성 유지, 콘솔 0

### 다음 단계
- 사용자 체감 확인 — 대비(_HatchDarken)·간격(_HatchPeriod)·기울기 방향 취향 조정 여지. 과하면 오브젝트 이 존재감을 위해 배경만 끄기(오브젝트 비활성)도 1클릭
- **[23:24] 대비 확정 0.8** — 0.94 정리안이 "사라진" 것으로 체감(사용자) → 스크린샷에서 보였던 0.8로 복원. 0.94는 모니터 따라 비가시 수준이라 폐기

## [23:27] 탈출구 아치형 실루엣 (ExitArch 셰이더)

### 완료된 작업
- `ExitArch.shader` 신규 — 아치 SDF(위 반원 + 아래 사각의 합집합, 반지름 = 폭의 절반, 바닥 평평) + 4종 공통 보일 문법(쿼드 확장·양방향 요철·어두운 톤 테두리·인스턴스 시드). `_CornerRadius`는 아치에서 미사용(Inspector 블록 순서 유지용 주석 명시)
- `ExitArch.mat` 생성 → **Exit 렌더러 9곳**(레벨 프리팹 8개 baked Exit + Gimmicks/Exit.prefab)에 PrefabUtility(LoadPrefabContents→SaveAsPrefabAsset) 경유 적용 — 에디터 네이티브 경로라 열린 에디터와 충돌 없음
- **콜라이더/트리거 무변경** — 쿼드 안에서 실루엣만 아치로 그림(클리어 판정 동일)

### 코드 설명
#### 복잡한 코드 설명
- **픽셀 좌표계 SDF**: `uvC/fwidth(uvC)` = 콜라이더 기준 px 좌표 — 비균등 스케일에서도 아치 원이 진짜 원으로 유지됨(UV 좌표면 타원이 됨)
- **합집합 = min(sdRect, sdCirc)**: 반원과 몸통 사각을 각각 SDF로 만들어 min — 어깨선(반원·사각 접합부)이 수학적으로 연속

### 검증
- 콘솔 0. 인게임 실측(`exit-arch-check.png`): Level_1_1 출구가 초록 아치(돔) + 테두리 + 보일로 렌더, 접지 평평

### 다음 단계
- 사용자 룩 확인 — 아치 비율(현재 1×1 콜라이더 = 폭 절반 반원). "더 문처럼" 원하면 Exit 스케일을 세로로 키우는 옵션(콜라이더 변화라 판정 영향 — 별도 결정)

## [23:46] 리플레이 완주 연출 교체: 출구 흡입 → 제자리 페이드아웃 (사용자 결정)

### 완료된 작업
- 배경: 사용자가 Exit를 세로로 키우자 흡입(콜라이더 중심으로 위치 보간)이 히어로를 문 한가운데로 "띄우는" 부작용 → **문에 닿은 자리에서 점점 사라지는 것으로 교체**
- `ReplayDirector.cs` 수정 4곳: ① BeginAbsorb — 위치 캡처 제거(가드·타이머 유지) ② DriveAbsorb — 위치 Lerp → `_hero.Playback.SetDisplayAlpha(1−SmoothStep)` 알파 보간, 완료 시 0 스냅 ③ ResetForLoop — 루프 재시작 시 알파 1 복원(위치는 ResetToStart가 되돌리지만 표시 알파는 별도 채널) ④ StopReplay — 페이드 도중 스킵 시 반투명 잔존 방지 복원. 고아 필드(_absorbStartPosition/_absorbTarget) 제거
- `_absorbDuration`(0.35s)은 페이드 시간으로 의미 재사용(Inspector 튜닝 유지). SetDisplayAlpha는 기존 클론 리빌 인프라 재사용(RGB 보존·알파만)

### 검증
- 컴파일 0. **리플레이 완주 실측은 사용자 플레이테스트 몫**(레벨 클리어 실플레이 필요) — 체크 포인트: ① 완주 시 제자리 페이드아웃 ② 루프 재시작 시 정상 재등장 ③ 페이드 중 스킵 시 히어로 불투명 복원

### 다음 단계
- 사용자 리플레이 플레이테스트 → 오늘 전체 커밋

## [23:53] 페이드아웃 미동작 수정 (SetDisplayAlpha 즉시 반영)

### 완료된 작업
- 사용자 "완주 순간 멈춰만 있고 안 사라짐" — 원인: `ClonePlayback.SetDisplayAlpha`는 값 저장만 하고 렌더러 반영은 `UpdateReveal`(ApplyTick 경유)이 담당하는데, **페이드 분기는 틱 구동을 early-return으로 멈춘 상태**라 알파가 화면에 도달하지 않음
- 수정: `SetDisplayAlpha`가 저장 + **즉시 렌더러 반영**(UpdateReveal과 동일식 `_displayAlpha × _revealProgress`) — 알파 소유권은 여전히 ClonePlayback 단독(소유자 내부 수정이라 규약 유지), 이후 ApplyTick과 같은 값이라 충돌·번쩍임 없음
- 부수 해소: StopReplay/ResetForLoop의 알파 복원도 같은 저장-만 문제를 잠재했었음 — 동일 경로로 함께 해결

### 검증
- 컴파일 0. 콘솔의 "missing script" 에러 1건은 TextBoil 삭제 직후 도메인 리로드의 일회성 잔재로 확인 — 씬·전체 프리팹 스캔 결과 missing 0
- 페이드 실동작은 사용자 플레이테스트 몫(완주·루프·스킵 3점)

### 다음 단계
- 사용자 리플레이 재확인 → 커밋

## [23:57] 출구 입력 게이트 + ↑ 유도 프롬프트 (클리어 조건 변경 — 사용자 결정)

### 완료된 작업
- **클리어 조건 변경**: 트리거 접촉 즉시 클리어 → **범위 안에서 ↑/W 입력 시 클리어**. `LevelExit.cs` 개편: OnTriggerEnter 즉시 클리어 제거, Enter/Exit로 범위 추적 + Update에서 입력 검사(Input System `Keyboard.current`, `wasPressedThisFrame`). OnCleared 이벤트·멱등 통지·라이브 전용 규약 유지, RoundManager 0줄
- **유도 프롬프트**: 범위 진입 시 문 위 ↑ 화살표(생성 스프라이트 `Assets/Art/Temp/UpArrow.png` 64px, EnvironmentBoil 보일 적용, 진회색) 표시 + 사인 둥실거림(진폭 0.08/2.2Hz Inspector 튜닝). Recording 상태에서만 표시
- **스케일 무관 배치**: Start에서 콜라이더 bounds 기준 위치 + lossyScale 역보정 — 사용자가 Exit를 세로로 키워도 화살표 안 찌그러짐
- **stale 참조 방어**: bool 대신 범위 안 액터 참조 저장 + 매 프레임 LiveCharacter 동일성 재검증 — 범위 안에서 클론 확정 시 새 라이브가 밖에 있어도 오발동하는 잠복 버그 차단
- 배선: 에디터 스크립트로 프리팹 9곳(레벨 8+Exit.prefab)에 ExitPrompt 자식 생성 + _promptRenderer 연결

### 검증
- 컴파일 0, 콘솔 0. 스모크 실측: 히어로 텔레포트 → 프롬프트 표시(위치·스케일 0.5 정상, `exit-prompt-check.png`), 이탈 → 즉시 숨김. **↑ 입력 클리어와 리플레이 연계는 사용자 플레이테스트 몫**
- 유의: 클라이머 벽타기 위 입력과 키가 겹치나 출구는 지상·비Climbable이라 실충돌 없음

### 다음 단계
- 사용자 플레이테스트(↑ 클리어 감각 + 프롬프트 가독성) → 커밋

## [00:10] "다음 스테이지 안 넘어감" 원인 수정 (페이더 예외 고착) + 관찰 항목

### 완료된 작업
- 사용자 "다음 스테이지가 제대로 안 넘어감" — 실측 재현·원인 확정: **레벨 전환 콜백(LoadLevel) 도중 예외 발생 → ScreenFader 코루틴 사망 → `IsFading` true 영구 고착 → 이후 모든 LoadNextLevel/ReturnToTitle이 가드에 걸려 조용히 무시**. 실측 당시 콘솔에 `CharacterActor.SetMode` NRE 1건(Initialize→EnterSelecting 경유, _rigidbody null = Awake 미실행/파괴 액터 추정)
- 수정: `ScreenFader.FadeRoutine`의 onCovered 콜백을 **try/catch**로 감싸 예외 시에도 페이드인·상태 복구를 보장(예외는 LogException으로 재노출). 전환 중 어떤 예외가 나도 게임이 영구 잠금되지 않음
- 답변: 스테이지 전환 주체 = `LevelManager.LoadNextLevel()`(Cleared 상태에서 Enter/N/Next 버튼, `_levels` 배열 순환, 페이드 경유) — 배열 순서·프리팹 참조는 실측 정상

### 관찰 항목 (미해결 — 자동 테스트 중 관측, 사용자 입력 개입 가능성으로 판단 유보)
- **동일 레벨 이중 로드 1회 관측**(Level_1_2 ×2 + 캐릭터 누적) — 클리어와 Next를 같은 프레임에 인위 호출한 패턴 포함, 재현 조건 불명. 실사용 패턴(클리어→리플레이→Next)은 2회 실측 모두 정상
- **DriveGimmicks NRE**(`_gimmicks` null 상태 FixedUpdate) — HANDOFF 기존 "[원인 미상 1회 관측] Rewind NRE"와 동일 서명, 이중 로드 오염 상태에서 재등장. 근본 원인 추적 필요
- **"missing script" 에러 재출현 1회** — 에디터 스캔(씬·전체 프리팹)은 0. 도메인 리로드 일회성 추정
- **⚠ 워크플로우 함정**: 플레이 도중 스크립트 수정+에디터 포커스 복귀 = 도메인 리로드 → LevelManager 비직렬화 상태(_currentIndex 등) 초기화로 진행 꼬임. **플레이테스트 중에는 코드 수정 반영을 멈추거나, Preferences > Script Changes While Playing = Recompile After Finished Playing 권장**

### 다음 단계
- 사용자 단독 클린 플레이로 전 레벨 전환 확인(제 개입 없이) → 이상 시 콘솔 에러 원문 확보

## [00:44] Unity MCP 세션 잠금 훅 (멀티 세션 동시 조작 방지 — 사용자 결정)

### 완료된 작업
- 배경: "다음 스테이지 안 넘어감" 계열 이상의 유력 원인이 멀티 세션 동시 Unity 조작(플레이 토글 충돌·이중 로드·리컴파일 상태 초기화)으로 판단됨(사용자 통찰). 심플한 상호 배제만 도입(작업판·설명 실기는 사용자가 불요 판정)
- `.claude/scripts/unity-mcp-lock.ps1` 신규 + `.claude/settings.json` 훅 3종:
  - **PreToolUse**(`mcp__UnityMCP__.*`): 락 없음/자기 락 → 갱신·통과, 타 세션 유효 락 → exit 2 차단(차단 메시지에 행동 지침 포함 — 파일 작업 우선/보고 후 대기/자동 해제 안내)
  - **Stop·SessionEnd**(`-Release`): 자기 락 삭제 → 턴 종료 즉시 다른 세션이 이어받음
  - TTL 15분(크래시 잔존 락 자동 무시)
- `.gitignore`에 락 파일 추가, CLAUDE.md 개발 인프라에 규칙 1줄(우회 금지 포함)
- ⚠ 함정 기록: **한글 포함 .ps1은 UTF-8 BOM 필수**(PS 5.1이 BOM 없으면 ANSI 오독 → 파싱 실패). settings.json의 한글 statusMessage도 PS 검증기와 충돌해 ASCII로 교체

### 검증
- 파이프 테스트 4케이스(획득/타 세션 차단/자기 락 통과/해제) 전부 통과, JSON 스키마 검증 통과, 기존 셸 차단 훅 보존
- **실발화 확인**: Unity MCP 호출 → 훅이 현 세션 ID로 락 생성(재시작 불요, 즉시 가동)

### 다음 단계
- 멀티 세션 운용 시 실전 확인 (차단 메시지·교대 체감)

## [21:24] 내부 노이즈 얼룩 수정 (코너 라운딩 거리 캡 버그)

### 완료된 작업
- EnvironmentBoil·HatchedObstacle 코너 라운딩을 **모서리 영역(두 변 모두 반지름 이내) 한정 적용**으로 수정

### 코드 설명
#### 복잡한 코드 설명
- **버그 원인**: 무조건 `min(edgePx, cornerD)`로 합치면 내부 깊은 곳에서 거리가 반지름(4.7px)으로 캡됨 → 반지름 < 테두리 폭(5px)이라 **내부 전체가 테두리 밴드 판정**에 들어가고, 요철 노이즈(경계 전용이어야 할)가 내부 색을 얼룩지게 함. 빗금도 interior 마스크에 눌려 흐려짐. 파라미터 통일(테두리 3→5px)로 표면화된 잠복 버그 — 반지름(4.7) > 테두리(3)였던 동안엔 캡이 테두리 판정 밖이라 무해했음
- **수정**: 모서리 영역 밖에서는 원래 거리(min(pxDist)) 유지 — 영역 경계에서 호 거리와 연속이라 이음새 없음

### 검증
- 콘솔 0. Level_1_4 실측(`envboil-clean-interior.png`): 박스·출구 내부 민면 복원, 문 빗금 선명, 테두리·라운딩·보일 유지

### 다음 단계
- 사용자 종합 룩 확인 → 확정 시 커밋

## [22:29] 레벨 공통 오브젝트 6종 프리팹화 (Gimmicks 폴더 보강)

### 완료된 작업
- 전 레벨 프리팹(1_1~1_8, Level_Ending) 하이어라키 스캔 → Gimmicks 폴더에 없는 baked 오브젝트 유형 6종을 `Assets/Prefabs/Gimmicks/`에 신규 프리팹으로 생성 (사용자 요청 "땅도 포함")
  - `Ground.prefab` — 표준 바닥 (Level_1_1 원본, Ground 레이어, scale 43.18×2.67, TerrainBoil)
  - `Door.prefab` — 압력판 연동 문 (Ground 레이어, 0.4×6.0, HatchedObstacle)
  - `PressurePlate.prefab` — 압력판 (Ground 레이어, 1.5×0.3, EnvironmentBoil, 트리거 콜라이더·눌림 연출 값 보존)
  - `Exit.prefab` — 출구 (Default 레이어, EnvironmentBoil)
  - `Ledge.prefab` — 범용 검정 지형 블록 (Level_1_2 원본, Ground 레이어, 2.0×0.4, TerrainBoil) — GroundBlock/StartBlock/LandBlock/Backstop/벽 등 동일 구성 블록의 대표
  - `ClimbingPillar.prefab` — 벽타기 기둥 (Climbable 레이어, 0.4×3.6, EnvironmentBoil)
- 레벨 프리팹 본체는 무변경 (HANDOFF 확정 사양 "baked 프로토타입 유지" 준수 — 프리팹 인스턴스로 전환하지 않음)
- PushableBox는 기존 `Prefabs/Level/PushableBox.prefab` 존재로 제외, SpawnPoint는 컴포넌트 없는 마커라 제외

### 코드 설명
#### 복잡한 코드 설명
- **LoadPrefabContents(읽기 전용) → 자식 복제 → SaveAsPrefabAsset → Unload(저장 안 함)** 방식 — 현재 Level_1_1이 프리팹 스테이지로 열려 있어, 스테이지/레벨 본체를 건드리지 않고 프리뷰 씬 안에서만 복사본을 만들어 저장
- 프리팹 루트 localPosition은 0으로 정규화 (설치 시 배치자가 위치 지정 — 기존 기믹 프리팹 계약과 동일)
- PressurePlate의 `_door` 참조는 프리팹 경계 밖 오브젝트라 에셋에서 자동 null — 설치 시 Inspector 재연결 필요 (ToggleSwitch→TimedDoor 계약과 동일)

### 검증
- 6개 전부 생성 OK (레이어·스케일·머티리얼 로그 실측), 콘솔 에러·경고 0

### 다음 단계
- 새 레벨 제작 시 이 프리팹들을 중첩 인스턴스로 설치 (기존 레벨 1_1~1_5 baked는 그대로)

## [22:46] 지형 안착 스냅 툴 (Snap To Ground, 에디터 전용)

### 완료된 작업
- `Assets/Editor/SnapToGroundTool.cs` 신규 — 메뉴 `Tools > Snap To Ground` + 단축키 `Ctrl+Shift+D`. 선택 오브젝트를 발밑 지형(x범위가 겹치는 아래쪽 스프라이트 중 가장 높은 윗면)에 y만 이동해 안착. 다중 선택 지원, `Undo.RecordObject`로 Ctrl+Z 가능. Editor 폴더라 빌드 자동 제외, 런타임 코드 0줄
- 배경: 레벨 꾸미기 시 Door 등을 땅 위에 딱 붙이는 수작업이 어렵다는 사용자 요청 (내장 V스냅/그리드 스냅으로 부족)

### 코드 설명
#### 복잡한 코드 설명
- **물리 레이캐스트 대신 SpriteRenderer bounds 계산** — 편집 모드/프리팹 스테이지에서는 물리 씬이 미동기화라 레이캐스트 신뢰 불가. `scene.GetRootGameObjects()` 순회라 프리팹 스테이지·프리뷰 씬에서도 동작
- **후보 필터**: x범위 겹침 + 윗면이 자기 중심보다 아래 — 옆에서 겹친 벽이나 천장에 붙는 오동작 방지. 자기 자신·자식은 제외
- **코어 로직 `SnapDown`은 public** — 메뉴 없이 스크립트/테스트에서 직접 호출 가능하도록 분리

### 검증
- 컴파일 에러·경고 0. 프리뷰 씬 실측(Level_1_1 읽기 전용 로드 — 본체 무영향): Door를 +2 띄우고 x도 이동 후 SnapDown → **바닥 y = -7.7100 = 지면 윗면 정확 일치(오차 0)**, x 보존 확인
- 부수 발견: Door 스프라이트 실높이는 6.75유닛(스케일 6 × 스프라이트 1.125) — "y = -7.71 + scale/2" 공식은 근사치, 스냅 툴은 bounds 기준이라 정확

### 다음 단계
- 실사용 체감 (PressurePlate는 관례상 0.06 묻힘 — 스냅 후 y -0.24 수동 조정 필요, 불편하면 예외 처리 추가 판단)

## [22:56] HUD 숫자 좌우 흔들림 수정 (고정폭 숫자 메시 이펙트)

### 완료된 작업
- 원인 실측: Bazzi 숫자 폭이 12~20px(size 40)로 제각각 + 중앙 정렬 → 숫자가 바뀔 때마다 문자열 폭이 변해 전체가 좌우 재배치(TimeValue 실측 ±6.5px 흔들림). 보일 셰이더 무관(꺼도 동일 — 사용자 확인). 이전 Pretendard는 1·7 외엔 균일 폭이라 덜 보였던 것
- `Assets/Scripts/UI/FixedDigitWidth.cs` 신규 — `BaseMeshEffect`로 Text 글리프 정점을 받아 **숫자만 균일 폭 슬롯(전체 숫자 최대 advance)에 재배치**. Bazzi 룩·중앙 정렬·UITextBoil 셰이더 그대로 유지
- SampleScene TimeValue/CloneValue/ElapsedValue 3개에 부착 + 씬 저장

### 코드 설명
#### 복잡한 코드 설명
- **슬롯 폭은 0~9 전수 최대 advance** — 현재 문자열에 없는 숫자가 나중에 나와도 폭이 일정해야 하므로 현재 문자만으로 계산하면 안 됨
- **폰트 메트릭→제너레이터 공간 환산은 실측 비율** — 캔버스 scaleFactor에 따라 제너레이터가 다른 크기로 굽기 때문에, 현재 문자열의 숫자 하나로 charWidth/advance 비율을 얻어 환산
- **공백은 쿼드를 안 만든다** (실측: "1 / 1" = 5글자 12정점) — 비공백 문자만 쿼드에 순서 대응. 초기 구현이 len×4 가정으로 CloneValue("0 / 3")에서 조기 종료했던 버그를 이걸로 수정
- **정렬 보정**: 중앙 정렬이면 폭 변화의 절반을 되돌려 중앙 고정 — 숫자 개수가 같으면 새 전체 폭이 상수가 되어 흔들림 소멸. 좌/우 정렬도 anchor factor로 지원
- **가드**: 리치 텍스트·개행·쿼드 매핑 불일치 시 무동작(원본 유지)

### 검증
- 컴파일 0. 실제 ModifyMesh 코드 경로 시뮬레이션(제너레이터 정점 → VertexHelper) 실측:
  - TimeValue 비대칭 샘플 "10.44"/"44.10"/"01.23" — 소수점 x: pre −6.5/+6.5/−1.5(흔들림) → **post 0.0/0.0/0.5(고정)**
  - CloneValue "0 / 3"/"1 / 1"/"4 / 4" — '/' x: **post 전부 0.000**
- 잔여 한계: 전체 폭이 홀수일 때 제너레이터의 시작 pen 정수 반올림으로 **±0.5px 서브픽셀 오차**(64pt 기준 비지각 — 수용)

### 다음 단계
- 플레이에서 체감 확인 (녹화 중 남은 시간 카운트다운이 제일 눈에 띄는 지점)
