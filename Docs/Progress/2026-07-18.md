# 2026-07-18 작업 로그

## [시간 미기록] 오케스트레이션 병용 체제 확정 + Evaluator 반증 채점 개편 (인프라)

### 완료된 작업
- **fable+하네스 병용 체제**: `~/.claude/fable/fable.md`에 하네스 예외 조항 추가(`/harness` 진행 중 오케스트레이션 지침 미적용, 이중 계층 방지), `fable on` 전환. `CLAUDE.md` "오케스트레이션 운용 기준" 섹션을 병용 체제로 갱신
- **하네스 모델 배분** (`~/.claude/skills/harness/SKILL.md`, `harness-refine/SKILL.md`): Planner=메인 루프(Fable) 직접, Critic=Explore(model: fable), Generator=general-purpose(model: opus 강제 — 미명시 시 부모 Fable 상속으로 토큰 낭비되던 문제 수정)
- **Evaluator 개편** (점수 인플레 해소 — 99/100 연속으로 변별력 상실 진단):
  - 독립 분리: Agent(Explore, model: fable)로 실행, 입력은 원본 스펙+diff+체크리스트만 (Planner/Critic/Generator 산출물 미전달 → 자기 채점 편향 절단)
  - `.claude/harness-eval.md`: 스펙 감사 신설(체크리스트 누락 스펙 요구 1건당 -5), 적대 검증 17점 신설(반증 시나리오 3개 MCP 실측 의무), 기계적 4섹션 57→40점 축소, 만점 방지 조항(95+는 반증 3개 통과+스펙 감사 무결 시만), 반증 미수행 시 기능 충족 상한 50%

### 다음 단계
- 다음 하네스 실행에서 새 채점 체계 실효성 확인 (점수 하락이 정상)
- CLAUDE.md·harness-eval.md 변경분 커밋

---

## [시간 미기록] 공유 스폰 포인트 + 스태거 페이드인 (실행 에이전트 위임)

### 완료된 작업
- **씬**: `=== PLAYER ===` 하위에 `SpawnPoint` 빈 오브젝트 신설 (-8, -2). 캐릭터 3기 위치 통일. RoundManager `_spawnPoint` 참조 연결
- `Clone/CharacterActor.cs` (+8) — `OverrideSpawnPosition(Vector2)` 추가. Awake의 자기 위치 캡처는 폴백으로 유지
- `Managers/RoundManager.cs` (+75) — `_spawnPoint` 주입(Awake), 스폰 겹침 쌍 `IgnoreCollision` 관리(등록/분리복구/전체복구)
- `Clone/ClonePlayback.cs` (+48) — 스태거 페이드인 (`_revealProgress`, 거리 0.5 이탈 또는 tick>50 폴백, 0.15초 페이드)

### 코드 설명
#### 자료구조 선택 이유
- 겹침 쌍 = 재사용 `List<(Collider2D live, Collider2D clone)>` — 매 FixedUpdate 순회하므로 GC 회피. 역순 순회로 제거 중 인덱스 안전

#### 복잡한 코드 설명
- **타이밍 불변 원칙**: "늦게 스폰"은 궤적 전체를 밀어 압력판 협력 타이밍을 깨뜨림 → 클론은 tick 0부터 정확히 재생하고 **알파만 늦게** 올림 (`UpdateReveal`)
- **분리 마진 0.05**: 겹친 채 IgnoreCollision을 복구하면 솔버가 라이브를 홱 튕겨냄 → 살짝 떨어진 뒤 복구
- **RestartTake에도 전체복구 추가** (에이전트 판단, 타당): EnterSelecting을 거치지 않고 SelectCharacter로 직행하는 경로라 복구 누락 시 쌍이 중복 누적됨
- **복구 누락 = 최대 리스크**: 남으면 이후 "클론 밟기"가 영영 안 됨 → EnterSelecting/Rewind/ConfirmClone/RestartTake 4개 진입점 전부에서 전체복구

### 검증 (MCP 실측)
- 컴파일 에러 0
- 3기 SpawnPosition 전부 (-8,-2) / 겹침 등록 pairCount=1·ignoring=True / 분리 후 1스텝 복구(GetIgnoreCollision=False)
- 페이드 0.000→0.067→…→0.500 (8틱≈0.15s, 미역행), tick>50 폴백 경로도 확인
- 15초 자동확정→EnterSelecting에서 전체복구 확인

### 레벨별 스폰 세팅 구조 (설계만 — 구현은 Phase 2b)
- 레벨 프리팹(1-1, 1-2…)마다 자기 `SpawnPoint`(+ Exit, 지형, 압력판)를 자식으로 포함
- LevelManager가 레벨 로드 시 `RoundManager._spawnPoint`에 해당 레벨의 SpawnPoint를 주입 (현재 Awake 주입을 "레벨 로드 시 주입"으로 확장)
- 사용자는 레벨 프리팹 안에서 SpawnPoint만 끌어 옮기면 배치 끝

### 다음 단계
- 사용자 플레이테스트 (스태거 연출 눈 확인 + ⭐재미 검증)
- Phase 2b — 레벨 프리팹 시스템 + LevelManager + 검증 레벨 3~5개

---

## [시간 미기록] Phase 2b — 레벨 프리팹 시스템 + 검증 레벨 3종 (하네스, 99/100 PASS)

### 완료된 작업

**신규**
- `Level/LevelDefinition.cs` (29줄) — 레벨 프리팹 루트 데이터 홀더 (IdentityData[]/SpawnPoint/LevelExit/레벨명)
- `Managers/LevelManager.cs` (103줄) — 레벨 로드/언로드, 캐릭터 동적 생성+정체성 주입, N키 순환
- `Prefabs/Level/Level_1_1~1_3.prefab` — 1_1 기본협력 [Heavy,Light] / 1_2 클론밟기 [Light,Light] / 1_3 ⭐순서강제 [Heavy,Light,Light]

**수정 (Edit 전용)**
- `RoundManager.cs` 504→550 — 런타임 주입 리팩터: `Initialize`/`Teardown`/`_initialized` 가드 7개. **보호 블록(ResolveCrush/Push/중앙 틱/IgnoreCollision) diff 0**
- `CharacterActor.cs` +9 — `InjectIdentity` (필드 대입만)
- `LevelExit.cs` +9 — `BindRoundManager` (프리팹→씬 참조 불가 우회)
- `CharacterSelectUI.cs` +14 — `i >= SlotCount` 버튼 숨김 + Cleared "N키" 안내
- `SampleScene.unity` -1030 — 기존 직접 배치 전부 레벨 프리팹으로 이관. Ground/Canvas/RoundManager/그룹 유지, LevelManager+`=== LEVEL ===` 추가

### 코드 설명

#### 비직관 로직 (핵심 3개)
- **비활성 부모 트릭** (R1): `=== PLAYER ===` SetActive(false) → Instantiate(**Awake 지연**) → InjectIdentity → SetActive(true) → Awake가 주입된 정체성으로 실행. Awake 무수정으로 동적 생성 성립 — 유일한 방법 (Critic 판정)
- **Teardown 순서 함정**: `_liveIndex=-1`을 `_characters=null`보다 먼저 (레벨 전환 순간 LevelExit이 LiveCharacter를 읽으면 NRE) + 3컬렉션(`_confirmedSlots`/`_originalParents`/`_ignoredSpawnPairs`) 전부 Clear (누락 시 다음 레벨 Rewind가 파괴된 Transform으로 SetParent)
- **캐릭터 파괴는 리스트 기반**: 확정 클론이 `=== CLONES ===`로 reparent돼 있어도 `_spawnedActors` 리스트로 부모 무관 전부 Destroy

#### Level_1_3 순서 강제 증명 (Evaluator 실측)
- Light 선행 → 판 무게 1≪10, 문 안 열림 (실측 IsOpen=False) → 좌측 갇힘 ∴ Heavy 1위
- 등반 Light 선행 → 지면 apex 실측 0.706 < 렛지 1.2 ∴ 스툴 먼저
- Heavy Exit 시도 → 지면 -0.886 / 클론 위 0.134 (둘 다 <1.2) ∴ 마지막은 Light
- **역할 순서 [무게게이트→스툴→등반] 물리적으로 엄격 강제**

### 검증 (Evaluator MCP 실측 — 99/100)
- 컴파일 0 / 보호 블록 diff 0 / 주입 실측(Heavy 10/256, Light 14/768, mass 1) / 레벨 전환 잔존 0 / 1_2 렛지 게이트 양방향(0.706 < 1.2 < 1.726) / 순환 정상
- Minor 2건: `_initialized` 네이밍(→`_isInitialized`), UI 매 프레임 SetActive

### 다음 단계
- 사용자 플레이테스트 — ⭐**GDD 재미 검증 지점**: 1_3에서 "순서를 고민하며 클론을 배치해 푸는 게 재밌는가?"
- Minor 정리 + 커밋
- Phase 3 — 운반형/벽타기형 + 물체 밀기 + 환경 기믹 + 클리어 리플레이 연출(§9.3 협력 고리 고조)

---

## [시간 미기록] 세션 인수인계 체계(HANDOFF.md) 도입

### 완료된 작업
- `Docs/Progress/HANDOFF.md` 신설 — 항상 덮어쓰는 현재 상태 스냅샷 (현재 위치/다음 작업 우선순위+차단 관계/유효 제약/확정 사양·폐기한 접근/상세 이력 링크)
- `CLAUDE.md` 작업 로그 규칙에 "HANDOFF 스냅샷" 하위 섹션 추가 — 세션 시작 시 HANDOFF.md 우선 읽기, 날짜 로그(이력)와 HANDOFF(스냅샷) 역할 분리, 작업 완료 시 동시 갱신, 코드가 안 바뀐 결정(플레이테스트 결과·합의·폐기 접근)도 기록

### 다음 단계
- HANDOFF.md의 "다음 작업" 참조 (⭐Level_1_3 플레이테스트가 차단점)

---

## [시간 미기록] Phase 3-1 — 운반형 정체성 + 물체 밀기 (하네스, 99/100 PASS)

> 착수 전 ⭐재미 검증(Level_1_3 사용자 플레이테스트) 통과 확인 → Phase 3 진입.

### 완료된 작업

**신규**
- `Level/PushableBox.cs` (141줄) — Frozen/Record/Replay 3모드 박스. `CloneRecording`/`RecordedFrame` 재사용, tick+1 재생, 자체 FixedUpdate 없음(중앙 틱 구동), `_weight`는 압력판 전용(mass 1 불변)
- `Data/Identities/Identity_Carrier.asset` — 운반형 (moveSpeed 6 / jump 12 / weight 3 / groundMask 256 / `_canManipulateObjects: 1`)
- `Prefabs/Level/PushableBox.prefab` — Dynamic RB2D, mass 1, FreezeRotationZ, layer Default(0), weight 10
- `Prefabs/Level/Level_1_4.prefab` — 검증 레벨: 운반형이 박스를 좌측 압력판에 밀어 문 개방 → 가벼운형 통과. 순서 강제 유지

**수정 (Edit 전용, 보호 블록 0-diff)**
- `Core/IdentityData.cs` +7 — `CanManipulateObjects` 플래그 (운반형 고유 동사 "옮긴다" 게이트)
- `Level/PressurePlate.cs` +10 — 합산 루프에 PushableBox 분기 (CharacterActor null-continue 앞, 동일 기하 필터)
- `Level/LevelDefinition.cs` +4 — `_boxes` 주입 배열
- `Managers/LevelManager.cs` 1/1 — Initialize 호출부 동반 수정 (유일 호출부)
- `Managers/RoundManager.cs` +101/-1 — `DriveBoxes`(중앙 틱 앞 삽입)/`PrepareBoxesForRound`(SelectCharacter)/박스 커밋(ConfirmClone)/소유 해제(Rewind)/프리즈(EnterSelecting)/Teardown `_boxes=null`
- `Scenes/SampleScene.unity` +1 — `_levels[3]`에 Level_1_4 등록

### 코드 설명

#### 자료구조 선택 이유
- 박스 궤적 = 클론과 동일한 `CloneRecording` 재사용 — DRY + 중앙 틱 단일 소유 규약 상속으로 클론/박스가 하나의 `_tick` 공유
- `_boxes`는 배열 — 레벨당 고정 크기, 순회만 하므로 충분

#### 비직관 로직
- **소유권 모델**: 미소유 박스는 라이브가 운반형일 때만 Record(Dynamic), 아니면 Frozen(Kinematic) — "운반형만 민다"를 물리 트릭이 아닌 데이터 플래그로 강제. 확정 시 변위 있으면 `CommitRecording`(OwnerSlot 기록), 이후 라운드에선 Replay로 100% 동기 재생
- **tick+1 규약**: `ApplyBoxTick`은 `Clamp(tick+1,…)` — ClonePlayback과 동일. 어기면 박스가 클론 손보다 1틱 지연
- **박스 레이어 Default(0)**: 압력판 마스크 513엔 포함(검출 정상), 접지 마스크(256/768)엔 미포함 → **박스 위 점프 불가. Level_1_4는 밀기 전용 설계** (박스 딛기는 레이어 정책 결정 후)

### 검증 (Evaluator MCP 실측 — 99/100, 동작 조건 12/12)
- 컴파일 0 / 보호 블록 0-diff / 로드 직후 박스 Frozen·Kinematic·미낙하·mass 1 / 판 위 박스 `MeasureWeightOnPlate=10 ≥ 10` → 문 개방 / Rewind 시 base 복귀·소유 해제 / Level_1_1~1_3 무회귀
- Minor 1건(-1): PressurePlate 핫패스 `GetComponentInParent<PushableBox>()` 트래버스 (영향 미미)

### 다음 단계
- 사용자 플레이테스트 — Level_1_4 박스 밀기 감각 + 클론·박스 동기 재생 체감
- 벽타기형 정체성 (코어 4종 완성)
- 박스 위 올라서기 레이어 정책 결정 (다중 박스 퍼즐 전제)

---

## [시간 미기록] Phase 3-2 — 벽타기형 정체성, 벽만 (하네스 v2 첫 실행, 90/100 PASS)

> 새 반증 채점 체계(독립 Evaluator + 스펙 감사 + 적대 실측)의 첫 회차. 이전 99점대와 직접 비교 불가 — 90점이 정상 범위.

### 완료된 작업

**신규**
- `Data/Identities/Identity_Climber.asset` — 벽타기형 (moveSpeed 6 / jump 10 / weight 2 / groundMask 256 / `_canClimbWalls: 1`)
- `Prefabs/Level/Level_1_5.prefab` — 검증 레벨: 클라이머가 기둥(Climbable)을 타고 벽면 중간 정지 → 클론이 발판 → Light가 밟고 렛지(top 3.0) 도달. 게이트 4종 실측 성립
- **Climbable 레이어(10) 신설** — "부착 가능한 벽"을 코드 판정이 아닌 레이어로 제한. 출구 렛지(Ground) 옆면 사다리 파훼를 마스크 수준에서 차단 (Critic 사전 탐지)

**수정 (Edit 전용)**
- `Player/PlayerController.cs` 137→251 — 벽 부착 상태: 매 틱 재검증(자가 치유), `Detach()`가 gravityScale 복원 단일 소유(+이중 가드), 부착 중 FixedUpdate 조기 return으로 기존 수평 이동 라인 바이트 불변, 벽점프 시 코요테/버퍼 소모, `_climbableLayer` 전용 마스크
- `Core/IdentityData.cs` +7 — `CanClimbWalls` 플래그
- `Prefabs/Player/Player.prefab` +5 — `_climbableLayer: 1024` 직렬화
- `Scenes/SampleScene.unity` +1 — `_levels[4]` / `ProjectSettings/TagManager.asset` — 슬롯 10

### 코드 설명

#### 자료구조 선택 이유
- 부착 상태 = `bool _isWallAttached` + `int _wallDirection`(±1) + `float _savedGravityScale` 최소 3필드. 상태가 2개뿐이라 enum 대신 bool (과설계 회피). 방향 int는 벽점프 반대방향·이탈 판정을 산술로 처리

#### 비직관 로직
- **자가 치유 재검증**: 부착 조건을 매 Update 재평가 — 어떤 경로로 깨져도(라운드 리셋 텔레포트 포함) 다음 틱 자동 이탈. RoundManager 특수 처리 0
- **Detach 단일 소유 + 이중 가드**: gravityScale 복원 코드가 프로젝트에 전무했음 — 부착 중 확정→되감기→재선택 시 0이 잔존해 둥둥 뜨는 잠복 버그를 OnDisable→Detach가 유일하게 방어. 미부착 호출 가드가 없으면 저장값 0으로 역오염
- **게이트 4종** (실측): Light 솔로 ✗(-2.2) / Light+지면 스툴 ✗(-0.76) / 클라이머 단독 ✗(기둥탑 접지 마스크 미포함 + 사거리 부족) / Light+벽면 클론 ✓ CLEARED

### 검증 (독립 Evaluator 실측 — 90/100)
- 적대 시나리오 6종 (되감기 인터럽트 / grav 오염 조합 / 홀드 점프 / 벽점프 수평 성분 / 마스크 우회 / 게이트 파훼)
- **[Major 잔존]** 벽점프 "반대 방향 성분"이 FixedUpdate 덮어쓰기에 무효화 (x변위 0.00) + 벽 방향 홀드 중 점프 무효(즉시 재부착). 완료 처리로 수용 — **추후 수정 항목: 재부착 락아웃 ~0.15s**
- Minor 3건: 이탈 블립 중 기둥탑 마운트 / 대각 주석 전제 / 기즈모 좌표 소스

### 다음 단계
- Level_1_4/1_5 플레이테스트 (박스 밀기 + 벽타기 조작감)
- 벽점프 락아웃 수정 (Major 해소)
- Phase 3-3 환경 기믹 6~8종

---

## [시간 미기록] fable 상시 병용 폐기 → 기본 OFF 확정 (인프라)

### 완료된 작업
- `fable off` 전환. CLAUDE.md 운용 기준·HANDOFF 확정 사양·메모리를 "fable 기본 OFF + 하네스" 체제로 갱신
- 유지되는 것: fable.md 하네스 예외 조항(일시적 `fable on` 시에도 하네스와 충돌 없음), 하네스 모델 배분(Critic·Evaluator=fable, Generator=opus — 스킬에 명시되어 토글 무관)

### 다음 단계
- HANDOFF "다음 작업" 참조 (Level_1_4/1_5 플레이테스트가 차단점)
