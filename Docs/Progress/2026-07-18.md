# 2026-07-18 작업 로그

## [시간 미기록] 공유 스폰 포인트 + 스태거 페이드인 (실행 에이전트 위임)

### 완료된 작업
- **씬**: `=== PLAYER ===` 하위에 `SpawnPoint` 빈 오브젝트 신설 (-8, -2). 캐릭터 3기 위치 통일. RoundManager `_spawnPoint` 참조 연결
- `Clone/CharacterActor.cs` (+8) — `OverrideSpawnPosition(Vector2)` 추가. Awake의 자기 위치 캡처는 폴백으로 유지
- `Managers/RoundManager.cs` (+75) — `_spawnPoint` 주입(Awake), 스폰 겹침 쌍 `IgnoreCollision` 관리(등록/분리복구/전체복구)
- `Clone/ClonePlayback.cs` (+48) — 스태거 페이드인 (`_revealProgress`, 거리 0.5 이탈 또는 tick>50 폴백, 0.15초 페이드)

### 코드 설명
#### 자료구조 선택 이유
- 겹침 쌍 = 재사용 `List<(Collider2D live, Collider2D clone)>` — 매 FixedUpdate 순회하므로 GC 회피. 역순 순회로 제거 중 인덱스 안전

#### 복잡한 코드 설명
- **타이밍 불변 원칙**: "늦게 스폰"은 궤적 전체를 밀어 압력판 협력 타이밍을 깨뜨림 → 클론은 tick 0부터 정확히 재생하고 **알파만 늦게** 올림 (`UpdateReveal`)
- **분리 마진 0.05**: 겹친 채 IgnoreCollision을 복구하면 솔버가 라이브를 홱 튕겨냄 → 살짝 떨어진 뒤 복구
- **RestartTake에도 전체복구 추가** (에이전트 판단, 타당): EnterSelecting을 거치지 않고 SelectCharacter로 직행하는 경로라 복구 누락 시 쌍이 중복 누적됨
- **복구 누락 = 최대 리스크**: 남으면 이후 "클론 밟기"가 영영 안 됨 → EnterSelecting/Rewind/ConfirmClone/RestartTake 4개 진입점 전부에서 전체복구

### 검증 (MCP 실측)
- 컴파일 에러 0
- 3기 SpawnPosition 전부 (-8,-2) / 겹침 등록 pairCount=1·ignoring=True / 분리 후 1스텝 복구(GetIgnoreCollision=False)
- 페이드 0.000→0.067→…→0.500 (8틱≈0.15s, 미역행), tick>50 폴백 경로도 확인
- 15초 자동확정→EnterSelecting에서 전체복구 확인

### 레벨별 스폰 세팅 구조 (설계만 — 구현은 Phase 2b)
- 레벨 프리팹(1-1, 1-2…)마다 자기 `SpawnPoint`(+ Exit, 지형, 압력판)를 자식으로 포함
- LevelManager가 레벨 로드 시 `RoundManager._spawnPoint`에 해당 레벨의 SpawnPoint를 주입 (현재 Awake 주입을 "레벨 로드 시 주입"으로 확장)
- 사용자는 레벨 프리팹 안에서 SpawnPoint만 끌어 옮기면 배치 끝

### 다음 단계
- 사용자 플레이테스트 (스태거 연출 눈 확인 + ⭐재미 검증)
- Phase 2b — 레벨 프리팹 시스템 + LevelManager + 검증 레벨 3~5개
