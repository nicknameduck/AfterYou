# 2026-07-12 작업 로그

## [시간 미기록] Phase 1 — 녹화·재생 코어 시스템 (하네스, 91/100 PASS)

### 완료된 작업

**신규 (8파일)**
- `Core/RecordedFrame.cs` (28줄) — struct {Position, IsFacingRight}. 궤적 1틱 분량
- `Core/CloneRecording.cs` (45줄) — {SlotIndex, List<RecordedFrame>}. Phase 3 undo 스택의 "한 층"
- `Clone/CharacterRecorder.cs` (60줄) — 틱마다 `frames.Add(rb.position)` 캡처
- `Clone/ClonePlayback.cs` (70줄) — Kinematic 클론이 `MovePosition(frames[tick+1])` 재현
- `Clone/CharacterActor.cs` (110줄) — 모드 전환(Idle/Live/Clone): bodyType·layer·컴포넌트 enabled
- `Managers/RoundManager.cs` (254줄) — 중앙 tick, 상태머신, 확정/재녹화/되감기, 클리어
- `Level/LevelExit.cs` (33줄) — 출구 트리거 (라이브만 판정)
- `UI/CharacterSelectUI.cs` (50줄) — 슬롯 버튼 3 + 상태 텍스트

**수정 (2파일, Edit만)**
- `Player/PlayerController.cs` 106→111줄 (+5 삽입, 0 삭제) — Awake에 `_inputActions = Instantiate(_inputActions);` 1줄
- `AfterYou.asmdef` — `UnityEngine.UI` 참조 추가

**씬**: 캐릭터 3기(-4/0/+4, y=-2), Platform(클론 밟아야 닿는 높이), Exit(isTrigger), RoundManager, Canvas+EventSystem(InputSystemUIInputModule)

### 코드 설명

#### 컨테이너/자료구조 선택 이유
- `RecordedFrame` = **struct**: 틱당 1개(15초×3클론 ≈ 2,250개) 누적 → 힙 할당·GC 압력 회피
- `List<RecordedFrame>` + `Add()`만 사용: `List`는 Count 초과 인덱스 **대입 불가**(`ArgumentOutOfRangeException`). 읽기는 `Mathf.Clamp`로 마지막 프레임 홀드 → 길이가 다른 클론들이 하나의 중앙 틱을 공유해도 안전
- 확정 스택 = `List<int>` 말단 push/pop: 되감기가 최근→과거 순서만 허용되므로 스택 시맨틱으로 충분

#### 복잡한 코드 설명 (핵심)
- **틱 오프셋 규약** (`ClonePlayback.cs:48-53`): FixedUpdate는 물리 시뮬 **이전**에 실행 → 현재 위치 = 틱 t의 *시작* 위치. `MovePosition`은 이번 스텝 동안 목표까지 이동 → 스텝 끝(= 틱 t+1 시작)에 도달. 따라서 틱 t에서 **`frames[tick+1]`**을 목표로 해야 라이브 궤적과 시점이 일치한다. `tick`을 그대로 쓰면 1틱 밀려 압력판/발판 타이밍 버그의 원인이 됨
- **공유 InputActionAsset 함정**: 캐릭터 3명이 같은 에셋을 참조하면 `OnEnable/OnDisable`에 레퍼런스 카운팅이 없어 전환 순서에 따라 액션이 최종 Disable → **2라운드부터 조작 불능**. `Instantiate(_inputActions)`로 인스턴스별 사본 확보 (Unity PlayerInput 공식 패턴)
- **접지 레이어 전환**: 접지 마스크가 Ground(8) 단독이라 클론(layer 0)은 접지로 인식 안 됨 → 클론 위에서 점프 불가. `CharacterActor`가 Clone→8, Live/Idle→0으로 전환. ⚠ 라이브를 8로 두면 GroundCheck가 자기 콜라이더를 잡아 무한 점프
- **bodyType 전환 순서**: Kinematic도 `linearVelocity`가 남으면 관성 이동해 MovePosition과 충돌 → 속도 0 → bodyType → layer → 컴포넌트 → SetActive 순서 엄수
- **좌표 소스**: 프리팹이 Interpolate라 `transform.position`은 보간값 → 기록/재생 모두 `Rigidbody2D.position` 기준

#### 함수 설명
- `RoundManager.FixedUpdate`: Recording일 때만 `playback.ApplyTick(tick)` → `recorder.CaptureTick(tick)` → `tick++` → 상한(751틱) 도달 시 자동 확정
- `RoundManager.SelectCharacter`: "전원 Idle → 확정 클론 복구 → 새 라이브" **2-패스** (액션 Enable/Disable 순서 의존성 구조적 제거)
- `CharacterActor.SetMode`: 캐릭터의 물리/입력/렌더 모드를 한 곳에서 전환
- `LevelExit.OnTriggerEnter2D`: 클론과 라이브가 같은 프리팹이라 태그/레이어로 구분 불가 → `RoundManager.LiveCharacter` **참조 동일성** 비교

### 검증 (MCP 실측)
- 컴파일 에러 0 / 워닝 0
- 틱 불변식 `tick == FrameCount` 항상 일치 (288/288, 481/481, 751/751), Assert 미발화
- **3라운드에서도 조작 정상** (치명 1 해결 확인)
- **점프 수치**: 지면(중심 -2.0) 정점 0.83 < Platform top 1.30 → 혼자선 **불가** / 클론 위(중심 -0.97) 정점 1.86 > 1.30 → **가능**. 레벨이 클론 밟기를 강제
- 15초 방치 → frames=751 자동 확정 ✓
- 클론이 Exit에 닿아도 클리어 안 됨 / 라이브가 닿으면 Cleared ✓

### 다음 단계
- Minor 7건 정리 (특히 Backspace 동작 스펙 확인, 0프레임 확정 가드)
- Phase 2 — 정체성 2종(무거운/가벼운) + 물리 규칙 2개 + 검증 레벨 3~5개 ⭐**재미 검증 지점**
