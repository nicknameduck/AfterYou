# 2026-07-28

## [시간 미기록] 종이 룩 폐기 → 노이즈 반전 룩 복원 (아트만, 게임플레이 유지)

### 완료된 작업
- 사용자 결정: 종이 질감 실험(4~13차) 전부 폐기 → "화면 지직거리는 노이즈" 상태(흰 배경 반전 룩 + 그레인 + 스캔라인)로 복원
- **백업**: 복원 직전 전체 스냅샷을 `experiment/paper-look` 브랜치에 커밋(`8301a99`) — 마음 바뀌면 되살릴 수 있음. main 작업 트리는 유지
- **git checkout (HEAD 원복)**: `Assets/Data/Identities/` 3종(잉크 틴트 → 원색), `Assets/Prefabs/Gimmicks/` 6종(잉크 팔레트+직렬화 상태 색+Paper 머티리얼 → 원복), `Assets/Prefabs/Level/`(PushableBox 앰버→주황 포함)
- `Player.prefab` — 머티리얼만 PaperSprite → Sprite-Lit-Default 원복. **`_groundCheckSize` 0.9(모서리 점프 수정)는 유지** (파일 내 선별 수정)
- 레벨 프리팹 지형 검정 재적용 (반전 룩): Level_1_2~1_7 Ground 레이어 지형 9개 → 검정 (기믹 컴포넌트·Climbable 자동 제외, 원래 반전 기록 "지형 9개"와 정확히 일치. Level_1_1은 Ground 지형 0개)
- SampleScene — 상주 바닥 PaperInk 챠콜 → Sprite-Lit-Default 검정, 카드 Inner 종이 타일 스프라이트 → 플랫(0.96), PaperBackground 쿼드 삭제. 카메라(흰 배경·y=2·ortho 11.4)·HUD·SelectBar·폰트(Pretendard-Bold)는 그대로
- `Renderer2D.asset` — FullScreenPass 피처 재활성 + passMaterial PaperOverlay → **ScreenScanline.mat** (7px 감산 0.035)
- `TapeWorld_VolumeProfile.asset` — FilmGrain **재활성** (Medium1 / intensity 0.35 / response 0.3 — 반전 룩 튜닝값), Vignette 0.16 → 0.28 (종이 시대 하향 철회)
- **종이 에셋 삭제** (백업 브랜치에 보존): PaperInk/PaperOverlay/PaperSprite .mat+.shader, PaperTexture.png(+Textures 폴더), NotoSerifKR Bold/Medium. 삭제 전 GUID 전수 검색으로 잔존 참조 0 확인
- **유지한 것(게임플레이·종이 시대 산출물 포함)**: 해금 시스템(LevelManager/LevelDefinition), 선택 모델 개편+카드 동적 생성(RoundManager/CharacterSelectUI), PaperHudUI(상단 3분할 HUD — 이름만 Paper, 기능은 룩 무관), 클리어 시간 정지, 모서리 점프 수정, 줌아웃(ortho 11.4), Screen Space Camera 캔버스, Pretendard 폰트, CloneAlpha 0.85, ScreenScanline/CloneGhost 셰이더

### 검증
- 콘솔 에러·경고 0 (에셋 삭제·리프레시 후)
- 스크린샷 `Assets/Screenshots/noise-look-restored.png` — 흰 배경 + 그레인 체감 + 검은 지형 + 비네트 확인
- 상주 바닥 실측: 폭 43.94 / 두께 0.97 / top y=-2.5 — 지오메트리는 손대지 않음(현 씬 상태 보존)

### 다음 단계
- 사용자 플레이 확인: 그레인/스캔라인 체감 강도 (그레인은 Volume, 스캔라인은 ScreenScanline.mat)
- 노이즈 룩 잔여(반전 룩 시절 미확정 항목): 클론 가산 스캔라인이 흰 배경에서 안 보이는 문제(감산 전환 제안됨), OSD/REC 글자 대비
- 확인 후 전체 변경분 커밋

## [시간 미기록] 캐릭터 선택 UI 좌측 아이콘 도크 전환 (하단 바 폐기)

### 완료된 작업
- `RoundManager.cs` — Selecting 입력에 위/아래 방향키 순환 이동(`MovePendingIdentity(±1)`) 추가, `EnterSelecting()` 끝에 `SetPendingIdentity(0)` 호출로 라운드 진입 시 첫 항목 자동 하이라이트. 1/2/3 숫자키·클릭 예비 선택 + Enter 시작은 기존 유지
- `CharacterSelectUI.cs` — 카드 라벨 텍스트 제거(아이콘 전용), pending 카드 하이라이트를 카드 루트 Image(=테두리) 색으로 표현: Selecting+pending = 진한 테두리(0.1 불투명), 그 외 = 옅은 테두리(alpha 0.2). `_cardLabels` 리스트 삭제, HelpLine에 ↑↓ 안내 추가
- SampleScene — SelectBar를 하단 전폭 바(높이 280) → **좌측 중앙 도크**(앵커 (0,0.5), x=24, 112×600)로 재앵커. TopLine/Heading/SideLineL/SideLineR 장식 비활성화(삭제 아님). CardContainer HorizontalLayoutGroup → VerticalLayoutGroup(spacing 16, MiddleCenter). CardTemplate 220×180 → 96×96, Icon 중앙 정렬, Inner 인셋 3px → 6px(테두리 하이라이트 가시성)

### 코드 설명
#### 컨테이너/자료구조 선택 이유
- 하이라이트 색은 `static readonly Color` 상수 2개 — 매 프레임 Update에서 비교/대입만 하므로 할당 없음
#### 함수 설명
- `MovePendingIdentity(int direction)`: `_pendingTypeIndex`를 ±1 모듈러 순환. 미선택 상태(-1)면 방향 무관 0부터 시작. 방향키 입력 전용
#### 복잡한 코드 설명
- 카드 루트 Image는 배경이 아니라 **테두리**다(Inner가 안쪽을 덮는 구조) — 흰 종이 배경 위에서 흰색 하이라이트가 보이지 않아, 테두리 진하기(알파)로 하이라이트를 표현

### 검증 (Unity MCP 실측)
- 컴파일 에러·경고 0
- 플레이 모드: Selecting 진입 시 pendingType=0 자동 하이라이트, `SetPendingIdentity(1)` 호출 시 하이라이트 이동, `SelectCharacter` 후 Recording 상태에서 pendingType=-1 + 전 카드 옅은 테두리 확인
- 스크린샷: `left-dock-highlight-check-1.png`(선택 중), `left-dock-move-check.png`(이동), `left-dock-recording-check.png`(녹화 중 꺼짐)

### 다음 단계
- 그라운드 화면 바닥 붙이기(레벨 프리팹/카메라 조정) — 이번 범위에서 제외됨
- 도움말 텍스트 위치 재검토(StatusText는 현재 비활성 상태)

## [시간 미기록] 바닥 화면 하단 부착 + 선택 도크 우측 하단 이동

### 완료된 작업
- SampleScene `Ground`(상주 바닥) — **top y=-2.5 고정**(레벨 배치 기준·물리 무영향), 아래로만 연장: scale y 0.97→7.5, pos y -2.98→-6.25 → 바닥 범위 y[-10, -2.5]. 화면 하단(y=-9.4, 카메라 y=2/ortho 11.4)까지 흰 여백 없이 채움. 카메라는 손대지 않음
- SelectBar — 좌측 중앙 도크 → **우측 하단 도크**: 앵커/피벗 (1,0), anchoredPos (-24,24), VerticalLayoutGroup childAlignment MiddleCenter→**LowerCenter**(카드가 바닥 띠 안에 아래부터 쌓임)
- `CharacterSelectUI.cs` — 하이라이트 색 반전: 검은 바닥 띠 위라 진한 테두리가 안 보임 → Highlight = 흰색 불투명, Normal = 흰색 alpha 0.15

### 검증 (Unity MCP 실측)
- 콘솔 에러·경고 0, ground top=-2.50 / bottom=-10.00 실측
- 플레이 모드: Selecting에서 위 카드 흰 테두리(pending=0)·아래 카드 0.15 알파 확인, 스크린샷 `floor-attached-rightdock-check.png` — 바닥이 화면 하단까지 채워지고 카드 도크가 우측 하단 띠 위에 앉음

### 다음 단계
- 사용자 플레이 확인: 우측 하단 도크 위치/크기 감각, 흰 테두리 하이라이트 가시성
- 도움말 텍스트를 바닥 띠 좌측에 배치할지 검토

## [시간 미기록] 바닥 레벨 프리팹 이관 — 공통 바닥 폐지 + 프리팹 좌표 재기준

### 완료된 작업
- 사용자 결정: 공통(씬 상주) 바닥으로는 레벨별 바닥 디자인(구멍/높이/폭)이 불가 → **바닥을 레벨 프리팹 소유로 이관**
- 사용자가 바닥을 top y=-7.71(화면 하단 얇은 띠, 40.53×1.68)로 직접 조정한 상태를 새 기준으로 채택
- 좌표 방식은 AskUserQuestion으로 **B안(프리팹 재기준, WYSIWYG)** 선택됨 — `=== LEVEL ===` 오프셋 없음
- Level_1_1~1_7 프리팹 7개 일괄 처리(PrefabUtility.LoadPrefabContents 스크립트): 직계 자식 전부 **y -5.21 시프트** + `Ground` 자식 추가(pos (0,-8.55), scale (40.53,1.68), Square 스프라이트/검정/Sprite-Lit-Default/Ground 레이어/BoxCollider2D)
- SampleScene — `=== ENVIRONMENT ===/Ground`(공통 바닥) 삭제

### 코드 설명
#### 복잡한 코드 설명
- 시프트량 -5.21 = 기존 기준선(top -2.5) → 새 바닥(top -7.71) 차이. 콘텐츠와 바닥이 함께 프리팹 안에 있으므로 상대 배치가 그대로 보존된다
- 접지/압력판은 레이어(Ground)·기하 판정 기반이라 코드 0줄 수정

### 검증 (Unity MCP 실측)
- 콘솔 에러·경고 0
- 플레이 모드에서 `LoadLevel(0~6)` 전 레벨 순회: **7개 전부 Ground top=-7.71**, 콘텐츠가 바닥 위 상대 배치 보존(압력판 top이 바닥 top과 플러시 등). Level_1_2는 공중 발판 레벨이라 콘텐츠 부양이 정상
- 주의: `LoadNextLevel()`은 Cleared 가드가 있어 검증엔 `LoadLevel(i)` 직접 호출 사용
- 게임 뷰 중앙의 떠 있는 아이콘은 씬 문제가 아니라 **게임 뷰 Gizmos 토글**로 인한 원점(0,0) 기즈모 아이콘(Global Light 2D/Volume) — 빌드 무관

### 다음 단계
- HANDOFF 유효 제약 갱신됨: 레벨 배치 기준선 top y=-2.5 → **top y=-7.71 (프리팹 내 Ground 소유)**
- 선택 도크 우측 하단 위치 사용자 확인 대기

## [시간 미기록] 정체성 아이디어 기록 — 트램펄린형 (문서만, 코드 변경 없음)

### 완료된 작업
- `Docs/team/GDD.md` — 확장 정체성 백로그(§5.2)에 **트램펄린형** 추가 (고유 동사 *띄운다*: 머리를 밟은 다른 클론을 높이 튕겨 올리는 능동 발판). §2 요약·§12 백로그의 "확장 4종" 표기를 5종으로 동기화
- 색상: 초록 후보였으나 탈출구와 겹쳐 **미정으로 보류** (사용자 결정)

### 다음 단계
- 도입 시점 결정 대기 (백로그 — 출시 후 업데이트 후보). 코어 4종 승격 여부는 사용자 판단

## [14:00] 노이즈(FilmGrain) 제거 + 캐릭터 접지 부양 수정

### 완료된 작업
- `Assets/Settings/TapeWorld_VolumeProfile.asset` — FilmGrain `active: 1 → 0` (사용자 "일단 아예 빼줘" — 값 0.35/0.3은 보존, 재활성만 하면 복귀)
- `ProjectSettings/Physics2DSettings.asset` — `m_DefaultContactOffset: 0.01 → 0.005`

### 원인 분석 (실측 기반)
- **노이즈**: 풀스크린 ScreenScanline.mat은 이미 강도 0·커브 0으로 무력화 상태였고, 실질 노이즈원은 FilmGrain 하나뿐이었다
- **캐릭터 부양**: 스프라이트(투명 여백 0)·콜라이더(1×1 일치)·SpawnPoint(-7.21 플러시)·Ground(콜라이더=스프라이트) 전부 정상. 원인은 Box2D가 정지 접촉을 Default Contact Offset만큼 띄워 유지하는 것 — 실측 rbY=-7.195(간극 0.015유닛 ≈ 0.7px). 흰 배경+검은 바닥 고대비라 이 틈이 눈에 띄었다
- 0.005로 축소 후 실측: 간극 0.005유닛(≈0.24px, 서브픽셀). 스크린샷 픽셀 검증에서 캐릭터 최하단 행 바로 다음 행이 지면색 — 흰 틈 0px

### 검증 (Unity MCP 실측)
- 흰 배경 픽셀 분산: 그레인 제거 전 min~240대 → 제거 후 R=253 균일
- 라이브 캐릭터 접지: `float-fix-live-verify.png` 픽셀 분석으로 틈 0px 확인, 콘솔 에러·경고 0
- 부수 관찰: 확정 클론은 Selecting 대기 중 알파 0(리빌 페이드 시작 전)이라 화면에 안 보이는 게 정상 동작

### 다음 단계
- 플레이테스트에서 스택/밀기 등 접촉 물리에 이상 없는지 체감 확인 (contactOffset 축소 영향권)
- 노이즈 룩 재도입 여부는 사용자 결정 대기 (FilmGrain active만 되돌리면 복원)

## [14:30] 선택 도크 카드 배경 반투명 검정 적용 (C안)

### 완료된 작업
- `Assets/Scenes/SampleScene.unity` — CardTemplate/Inner 이미지 색 `(0.96,0.96,0.96,1)` → `(0,0,0,0.85)` (방송 OSD 오버레이 느낌 — 사용자 A/B/C 중 C 선택)
- 흰 카드 배경이 흰 월드 배경에 묻히던 문제 해소. 코드 0줄 — 씬 값만 변경

### 검증 (Unity MCP 실측)
- 플레이 모드 스크린샷 `card-translucent-check.png` 픽셀 확인: 카드 안쪽이 어두운 톤으로 깔리고 정체성 아이콘 색 대비 유지, 흰 배경·검은 띠 양쪽에서 카드 실루엣 분리됨
- Normal 테두리(흰 α0.15)는 어두운 안쪽 위에서 회색 림으로 읽힘 — 정상(설계대로 카드 윤곽 역할)
- 콘솔 에러·경고 0. Level_1_7 프리팹 스테이지 열린 상태에서 작업 — dirty 아님 확인 후 플레이 진입

### 다음 단계
- 하이라이트(pending 흰 불투명 테두리)가 새 어두운 배경과 어울리는지 사용자 체감 확인

## [16:40] 선택 도크 상태 연동 표시/숨김 (녹화 중 숨김)

### 완료된 작업
- `Assets/Scripts/UI/CharacterSelectUI.cs` — Update()에 카드 컨테이너 토글 추가: Selecting에서만 표시, Recording/Cleared에서 숨김. `activeSelf != canSelect`일 때만 SetActive 호출(매 프레임 호출 방지)

### 코드 설명
#### 함수 설명
- 기존 Update() 상태 폴링 흐름에 3줄 삽입 — RoundManager 이벤트가 아직 없어(§9.3 잔여) UI는 폴링 구조를 유지한다
#### 복잡한 코드 설명
- 컨테이너가 비활성이어도 RebuildCards의 Instantiate(비활성 부모)와 카드 색 갱신은 정상 동작 — 숨김 중 상태가 바뀌어도 다음 표시 시점에 올바른 모습

### 검증 (Unity MCP 실측)
- 상태 검증: Selecting dockVisible=True → SelectCharacter → Recording dockVisible=False → 녹화 만료 자동 확정 → Selecting dockVisible=True (왕복 확인)
- 픽셀 검증: 도크 영역 비배경 픽셀 — 녹화 중 0 (`dock-hidden-recording-1.png`) / Selecting 1532 (`dock-visible-selecting.png`)
- 콘솔 에러·경고 0

### 다음 단계
- 클리어 상태에서도 숨김이 적절한지 사용자 체감 확인 (현재 Cleared에서도 숨김)
