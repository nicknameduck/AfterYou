# 하네스 로그 — 2026-07-12

## [Evaluator] Phase 1 — 녹화·재생 코어

- **판정: PASS (91/100)**
- 워크트리: OFF (본체 직접 수정)
- 대상: 신규 8파일 + PlayerController(+5줄) + asmdef + SampleScene + Player.prefab

### 섹션별 점수
| 섹션 | 배점 | 획득 |
|---|---|---|
| 코딩 컨벤션 | 18 | 17 |
| Unity 생명주기 | 13 | 11 |
| 성능 | 13 | 11 |
| Unity API 최신화 | 감점 | 0 |
| 안전성 | 13 | 11 |
| 기능 충족 | 23 | 23 |
| 회귀 방지 | 감점 | 0 |
| 파괴적 변경 | 감점 | 0 |
| MCP 런타임 검증 | 감점 | 0 |
| 단순성 | 10 | 8 |
| 완결성 | 10 | 10 |

### 핵심 판정
- Critic이 지목한 **치명 1(공유 InputActionAsset)** / **치명 2(접지 레이어)** 모두 해결. 플레이 모드 실측으로 확인.
- **틱 오프셋 규약(`tick + 1`)** 준수 + 근거 주석 명시. `tick == FrameCount` 항상 일치(Debug.Assert 미발화).
- 동작 조건 13개 전부 ✓. 컴파일 에러/워닝 0.
- Phase 2 정체성·carry 미구현 = 스코프 준수(위반 아님).

### MCP 런타임 검증 (플레이 모드 실행)
- 초기: 3기 모두 `Idle / inactive / 스폰 좌표`
- 선택: `Live / Dynamic / layer 0 / PC enabled`
- 확정: `Clone / Kinematic / layer 8 / Playback enabled / === CLONES === 하위`
- 3라운드에서도 `move.enabled=True, jump.enabled=True` (에셋 인스턴스 3개가 서로 다름)
- 클론 위 라이브 `_isGrounded = True`, OverlapBox → `Character_2 (layer 8)`
- 라이브 layer 0 공중 → OverlapBox NULL (무한 점프 역함정 회피)
- 15초 방치 → `frames=751` 자동 확정
- 클론을 Exit에 넣어도 미클리어 / 라이브를 넣으면 `state=Cleared`
- 점프 도달고: 지면 정점 하단 0.83 (Platform 1.30 미달) / 클론 위 1.86 (도달) → 레벨이 클론 밟기를 강제

### 잔여 Minor (7건)
1. `CharacterSelectUI.cs:44-47` Update 매 프레임 문자열 할당
2. `PlayerController.cs:47` Instantiate한 InputActionAsset의 OnDestroy 파괴 누락
3. `CharacterSelectUI.cs:26-31` onClick 구독 해제 없음
4. `RoundManager.cs:195-221` 녹화 중 Backspace가 테이크 폐기 + 스택 pop 동시 수행 (시맨틱 확인 필요)
5. `ConfirmClone` 0프레임 확정 엣지케이스 가드 없음
6. 미사용 public 멤버 4개 (`SpawnPosition`, `Frames`, `SlotIndex`, `CharacterRecorder.FrameCount`)
7. `CharacterRecorder.cs:48` Debug.Assert 미래핑 (실질 무해 — `[Conditional]`로 자동 제거)
