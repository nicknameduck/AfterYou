# 하네스 로그 — 2026-07-12

## [Evaluator] Phase 1 — 녹화·재생 코어

- **판정: PASS (91/100)**
- 워크트리: OFF (본체 직접 수정)
- 대상: 신규 8파일 + PlayerController(+5줄) + asmdef + SampleScene + Player.prefab

### 섹션별 점수
| 섹션 | 배점 | 획득 |
|---|---|---|
| 코딩 컨벤션 | 18 | 17 |
| Unity 생명주기 | 13 | 11 |
| 성능 | 13 | 11 |
| Unity API 최신화 | 감점 | 0 |
| 안전성 | 13 | 11 |
| 기능 충족 | 23 | 23 |
| 회귀 방지 | 감점 | 0 |
| 파괴적 변경 | 감점 | 0 |
| MCP 런타임 검증 | 감점 | 0 |
| 단순성 | 10 | 8 |
| 완결성 | 10 | 10 |

### 핵심 판정
- Critic이 지목한 **치명 1(공유 InputActionAsset)** / **치명 2(접지 레이어)** 모두 해결. 플레이 모드 실측으로 확인.
- **틱 오프셋 규약(`tick + 1`)** 준수 + 근거 주석 명시. `tick == FrameCount` 항상 일치(Debug.Assert 미발화).
- 동작 조건 13개 전부 ✓. 컴파일 에러/워닝 0.
- Phase 2 정체성·carry 미구현 = 스코프 준수(위반 아님).

### MCP 런타임 검증 (플레이 모드 실행)
- 초기: 3기 모두 `Idle / inactive / 스폰 좌표`
- 선택: `Live / Dynamic / layer 0 / PC enabled`
- 확정: `Clone / Kinematic / layer 8 / Playback enabled / === CLONES === 하위`
- 3라운드에서도 `move.enabled=True, jump.enabled=True` (에셋 인스턴스 3개가 서로 다름)
- 클론 위 라이브 `_isGrounded = True`, OverlapBox → `Character_2 (layer 8)`
- 라이브 layer 0 공중 → OverlapBox NULL (무한 점프 역함정 회피)
- 15초 방치 → `frames=751` 자동 확정
- 클론을 Exit에 넣어도 미클리어 / 라이브를 넣으면 `state=Cleared`
- 점프 도달고: 지면 정점 하단 0.83 (Platform 1.30 미달) / 클론 위 1.86 (도달) → 레벨이 클론 밟기를 강제

### 잔여 Minor (7건)
1. `CharacterSelectUI.cs:44-47` Update 매 프레임 문자열 할당
2. `PlayerController.cs:47` Instantiate한 InputActionAsset의 OnDestroy 파괴 누락
3. `CharacterSelectUI.cs:26-31` onClick 구독 해제 없음
4. `RoundManager.cs:195-221` 녹화 중 Backspace가 테이크 폐기 + 스택 pop 동시 수행 (시맨틱 확인 필요)
5. `ConfirmClone` 0프레임 확정 엣지케이스 가드 없음
6. 미사용 public 멤버 4개 (`SpawnPosition`, `Frames`, `SlotIndex`, `CharacterRecorder.FrameCount`)
7. `CharacterRecorder.cs:48` Debug.Assert 미래핑 (실질 무해 — `[Conditional]`로 자동 제거)

---

## [Evaluator] Phase 2a — 정체성 + 압력판 (평가일 2026-07-17)

- **판정: PASS (97/100)**
- 워크트리: OFF (씬 수정 포함 → 규정상 필수 OFF)
- 대상: 신규 3스크립트 + Identity 에셋 2종 + PlayerController(+26/-0) + CharacterActor(+38/-6) + SampleScene + TagManager
- **RoundManager.cs 미수정 확인** (429줄 유지, git status 클린)

### 섹션별 점수
| 섹션 | 배점 | 획득 |
|---|---|---|
| 코딩 컨벤션 | 18 | 18 |
| Unity 생명주기 | 13 | 13 |
| 성능 | 13 | 12 |
| Unity API 최신화 | 감점 | 0 |
| 안전성 | 13 | 12 |
| 기능 충족 | 23 | 23 |
| 회귀 방지 | 감점 | 0 |
| 파괴적 변경 | 감점 | 0 |
| MCP 런타임 검증 | 감점 | 0 |
| 단순성 | 10 | 9 |
| 완결성 | 10 | 10 |

### 핵심 판정
- Critic이 지목한 **치명 위험 9건 전부 해결**. 특히 C-3(무게/mass 분리)은 `.mass =` 대입이 코드베이스 전역 0건 + 런타임 `rb.mass=1` 실측으로 이중 확인.
- C-1(Awake 배치)은 **Idle/inactive 상태에서도 정체성이 주입돼 있다**는 실측이 곧 증거 — Start였다면 영영 실행되지 않았을 코드다.
- 동작 조건 16개 전부 ✓. 컴파일 에러/워닝 0.
- 운반형/벽타기형/물체밀기 미구현 = 스코프 준수(위반 아님).

### MCP 런타임 검증 (플레이 모드 실행)
- 런타임 정체성 주입: Heavy `jF=10 / moveSpeed=5 / groundLayer=256`, Light `jF=14 / moveSpeed=7 / groundLayer=768` (전부 Idle/inactive 상태에서 확인)
- `rb.mass=1` 3기 전부 / `gravityScale=3` — 무게 10이 mass로 새지 않았음
- 레이어: Clone→9, Live→0, Idle→0. `LayerMask.NameToLayer("Clone")=9`
- 색상: Heavy `RGBA(0.12,0.16,0.45)` / Light `RGBA(0.45,0.80,1.00)`
- **클론 위 가벼운형 `_isGrounded=True`** (overlap=`Character_2(layer 9)`)
- **클론 위 무거운형 `_isGrounded=False`** (overlap=NULL) — 단 y=-0.985·v=0으로 물리적으로는 서 있음 (확정 사양대로)
- 라이브 공중 → OverlapBox NULL (무한 점프 역함정 미재발)
- **Heavy CLONE이 압력판 → `IsOpen=True`** (라이브 전용 아님을 직접 증명)
- 판에서 이탈 → `IsOpen=False` 즉시 (홀드 스위치)
- Light×2 판 위 → `MeasureWeightOnPlate()=2` < 10 → `IsOpen=False` (마스크 513이 라이브+클론 **둘 다 검출**함을 동시 증명 — 미검출로 인한 가짜 통과가 아님)
- 점프 실측(`Physics2D.simulationMode=Script` 수동 스테핑): 지면 Light 정점 발바닥 **0.71** / 클론 위 Light **1.70** / 지면 Heavy **-0.89** (Platform top 1.30, Door top 3.50)
- 씬 지오메트리: Ground top -2.50 / Platform x[3,7] top 1.30 / Door x[1.30,1.70] y[-2.50,3.50] / Plate x[-6.75,-5.25] top -2.50 / Exit x[5.50,6.50] / 스폰 x = -8, -3, -1 (**3기 전부 문 앞쪽**)

### 잔여 Minor (3건)
1. `CharacterActor.cs:68-69` Clone 레이어 가드가 LogError만 하고 `-1` 대입을 막지 않음 (현 설정상 무해)
2. `PressurePlate.cs:113` FixedUpdate마다 `GetComponentInParent` (겹침 0~3개라 부담 미미, 2차 필터로 필수)
3. `IdentityData.cs:19,42` `DisplayName` 미사용 public 멤버
