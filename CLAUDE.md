# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**After You** — 타임 클론 레코드-앤-리플레이 퍼즐 플랫포머. (상세 기획: [Docs/team/GDD.md](Docs/team/GDD.md))

- **Engine**: Unity 6 (C#, 2D)
- **Platform**: Steam (PC) — 첫 상업 출시, 4,000~5,000원대
- **Multiplayer**: Singleplayer
- **Art Style**: TBD (프로토타입 후 결정 — 초반은 무채색 도형 임시 에셋)

### 한 줄 컨셉
*캐릭터 동작을 순차 녹화하면 "과거의 나" 클론들이 궤적을 재생 — 정체성별 역할을 나눠 협력해 출구로 나가는 퍼즐 플랫포머.*

### 게임 정체성
- **장르**: 타임 클론 / 레코드-앤-리플레이 퍼즐 플랫포머 (2D 횡스크롤)
- **핵심 메커닉**: 순차 녹화 → 클론 재생 → 정체성(무거운/가벼운/운반/벽타기) 협력. 물리(밟기/밀기/압력판)는 보조 규칙
- **핵심 루프**: 캐릭터 선택 → 조작+녹화 → 확정(undo 스택 push) → 클리어 판정 → 전 클론 동시 재생 연출
- **핵심 검증 질문**: "순서를 고민하며 클론을 배치해 퍼즐을 푸는 게 재밌는가?" (Phase 2에서 검증)
- **차별화 원칙**: 두뇌 퍼즐 우선 + 넉넉한 실행 판정 (정밀 조작 스트레스 회피)

### 스코프 / 일정
- **MVP 기준**: 코어 4종 정체성 / 물리 규칙 3개 / 25~30레벨 / 2~3시간
- **개발 기간**: 1~2개월 (알바 병행, 실질 8주). Phase 2까지 2~3주 내 도달 → 재미 검증이 최우선
- **로드맵**: Phase 0(세팅) → 1(녹화·재생 코어) → 2(정체성+물리, ⭐검증 지점) → 3(코어 완성) → 4(콘텐츠) → 5(출시 준비)

### 개발 인프라 (LostPages에서 이관)
- **Unity MCP** 연동 (`.mcp.json` + `com.coplaydev.unity-mcp` 패키지) — Editor를 MCP로 제어
- **하네스 워크플로우** (`/harness`, `/harness-refine` + `.claude/harness-eval.md`, `.claude/dashboard/`)
- **셸 검색 차단 훅** (`.claude/scripts/block-search-in-shell.ps1`)
- 관련 프로젝트: LostPages(`D:\LostPages\`), CatLibrary(`D:\CatLibrary\`)

## 오케스트레이션 운용 기준 (fable 기본 OFF + 하네스)

fable 오케스트레이션 상시 병용은 폐기했다 — 일상 턴의 위임 오버헤드 대비 이득이 작다. 구현 파이프라인은 하네스가 담당한다.

- **fable은 기본 OFF** (`fable off`). 탐색·조사가 많은 세션에서 필요할 때만 일시적으로 `fable on` 한다. 켠 상태에서 하네스를 돌려도 fable.md의 하네스 예외 조항이 이중 계층을 방지한다.
- **중규모 이상 구현 작업은 `/harness`** — fable 토글과 무관하게 하네스가 구현을 담당한다.
- **하네스 모델 배분** — Planner(메인 루프 직접 수행)·Critic(model: fable)·Evaluator(model: fable)는 판단 품질 우선, Generator(model: opus)는 코드 대량 생성 토큰 절약. 스킬에 model이 명시되어 있어 fable 토글과 무관하게 적용된다. 상세는 하네스 스킬(SKILL.md)이 정의한다.
- **1~2파일 사소한 수정·조회·논의는 메인 루프 직접** — 작업이 작아 토큰 절대량이 미미하고, 위임 왕복이 오히려 낭비다.

## 베이스라인 원칙 (Karpathy 4원칙)

LLM 코딩 흔한 실수를 줄이기 위한 일반 가이드라인. **이 4원칙은 베이스라인이며, 프로젝트 특화 규칙(작업 응답 규칙, 변경 안전 규칙, 하네스 규칙 등)과 충돌 시 프로젝트 특화 규칙이 우선한다.**

> **트레이드오프**: 속도보다 신중함을 택한다. 명백히 사소한 작업은 판단으로 우회 가능.

### 1. 코드 작성 전에 생각하기 — 가정하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러내라

구현 전에:
- **가정을 명시한다.** 불확실하면 묻는다.
- **여러 해석이 가능하면 모두 제시한다 — 조용히 하나만 고르지 않는다.**
- 더 단순한 접근이 있으면 말한다. 필요하면 반박한다.
- 모호하면 멈춘다. 무엇이 모호한지 이름 붙이고 묻는다.

> 단, **하네스 진행 중 명확화는 Planner/Critic 단계가 담당**한다. 메인 루프에서는 작업 응답 규칙(작업 의도 → 승인)을 따른다.

### 2. 단순성 우선 — 문제를 해결하는 최소 코드. 추측성 기능 금지

- 요청한 것 외 기능 추가 금지.
- 단일 사용 코드에 추상화 도입 금지.
- 요청되지 않은 "유연성"이나 "구성 가능성" 금지.
- 일어나지 않는 시나리오에 대한 에러 처리 금지.
- 200줄로 쓴 게 50줄로 가능하면 다시 써라.

자기 점검: "선임 개발자가 이걸 보고 과도설계라고 할까?" 그렇다면 단순화한다.

### 3. 외과적 변경 — 필요한 곳만 건드린다. 너가 만든 쓰레기만 치운다

기존 코드 수정 시:
- 인접한 코드, 주석, 포맷을 "개선"하지 마라.
- 망가지지 않은 것을 리팩터하지 마라.
- 본인이 다르게 짤 거였더라도 기존 스타일에 맞춘다.
- 무관한 데드 코드를 발견하면 언급만 하고 삭제하지 마라.

본인 변경이 만든 고아 코드(불필요해진 import/변수/함수)만 제거한다. 기존 데드 코드는 요청 없이 삭제 금지.

테스트: **변경된 모든 줄이 사용자 요청에서 직접 추적 가능해야 한다.**

> 단, **변경 안전 규칙의 호출부 동반 수정은 예외**다. 함수 시그니처/타입 변경 시 호출부 미수정은 컴파일 에러를 유발하므로 반드시 함께 수정한다.

### 4. 목표 주도 실행 — 검증 가능한 성공 기준을 정하고 루프

작업을 검증 가능한 목표로 변환:
- "검증 추가" → "잘못된 입력 케이스를 정의하고, 그 케이스에서 올바르게 동작하는지 확인"
- "버그 수정" → "버그 재현 절차를 정의하고, 수정 후 재현되지 않는지 확인"
- "X 리팩터" → "리팩터 전후 동작이 동일한지 확인"

다단계 작업은 간단한 계획 명시:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
```

> Unity 프로젝트 검증은 **Unity 콘솔(`read_console`) + 플레이테스트**로 한다. 자동화 테스트(PlayMode Test) 강요 금지.
> 사용자 승인 후 검증 루프를 돈다 (작업 응답 규칙 준수).

---

**작동 신호**: 디프에 불필요한 변경이 줄어들고, 과도설계로 인한 재작성이 줄어들고, 명확화 질문이 실수 후가 아닌 구현 전에 나온다.

## 코드 스타일

### 기본 원칙
- SOLID 원칙과 Clean Architecture 준수
- DRY 원칙 준수: 코드 중복 방지, 기존 기능 재사용
- 단순성 유지: 복잡한 솔루션보다 간단한 솔루션 우선
- 상속보다 컴포지션 선호 (MonoBehaviour 컴포넌트 기반)

### C# 네이밍 컨벤션
- 클래스, 메서드, 프로퍼티: `PascalCase` — `PlayerHealth`, `GetDamage()`
- private 필드: `_camelCase` — `_currentHp`, `_moveSpeed`
- local 변수, 파라미터: `camelCase` — `enemyCount`, `spawnDelay`
- 상수: `PascalCase` — `MaxWaveCount`, `DefaultGold`
- bool 변수: `is/has/can` 접두사 — `_isAlive`, `_hasShield`, `canAttack`
- 인터페이스: `I` 접두사 — `IDamageable`, `IMovable`
- 업계 통용 약어(`HP`, `UI`, `AI`, `ID` 등)는 전부 대문자 유지

### Unity 특정 사항
- SerializeField로 Inspector 노출 시 private 유지: `[SerializeField] private int _maxHp;`
- UI는 이벤트 기반 처리, Update()에서 무거운 로직 금지
- 캐스팅: `as`, `is` 패턴 매칭 사용, 직접 캐스트 `(Type)` 지양
- null 비교: Unity 오브젝트는 반드시 `== null` / `!= null` 사용. `is null` / `is not null`은 Unity의 `==` 오버로드를 우회하여 Destroy된 객체를 감지 못함 → 버그 유발
- 파일 인코딩: UTF-8 BOM (Unity 표준)
- **Obsolete API 금지**: Unity 공식 문서에서 `[Obsolete]` 표시된 API는 사용하지 않는다. 대표 사례:
  - `FindObjectOfType<T>()` → `FindAnyObjectByType<T>()`
  - `FindObjectsOfType<T>()` → `FindObjectsByType<T>(FindObjectsSortMode.None)`
  - `Input.*` (구 Input System) → `UnityEngine.InputSystem.*`

### 디버그 코드 규칙
- 디버그 전용 코드(시각화, 로깅, 디버그 프로퍼티 등)는 `#if DEVELOPMENT_BUILD || UNITY_EDITOR` ~ `#endif`로 래핑
- 디버그 프로퍼티를 노출하는 쪽(예: `Monster.DebugSlotPosition`)과 소비하는 쪽(예: `DebugOverlay`)을 반드시 동일한 전처리기 지시문으로 래핑하여 짝을 맞춤
- 디버그 전용 클래스(예: `DebugOverlay`)는 파일 전체를 래핑
- 디버그 프로퍼티 네이밍: `Debug` 접두사 사용 — `DebugSlotPosition`, `DebugTargetUnit`
- 결과: 에디터 + Development Build에서 디버그 기능 사용 가능, Release Build에서 자동 제거

## 코드 리뷰 및 분석 워크플로우

코드베이스를 분석하거나 리뷰할 때 다음 규칙을 따른다.

### 1. 컨벤션 검사
- 소스 코드를 직접 수정하지 않는다.
- `CONVENTION.md` 파일을 생성하거나 업데이트한다.
- 컨벤션 위반 사항만 작성한다.

### 2. 최적화 및 개선 검토
- 소스 코드를 직접 수정하지 않는다.
- `OPTIMIZATION.md` 파일을 생성하거나 업데이트한다.
- 성능, 구조, 유지보수 관점의 개선 사항만 작성한다.

### 공통 규칙
- 컨벤션 위반과 최적화 제안을 하나의 파일에 섞지 않는다.
- 모든 항목에는 파일 경로, 줄 번호, 문제 내용, 이유, 개선 방향을 포함한다.
- 문제가 없을 경우 "문제 없음"이라고 명시한다.
- 동일한 파일, 동일한 문제는 중복 보고하지 않는다.

### 우선순위 규칙
- 컨벤션 위반은 최적화 제안보다 우선한다.
- 컨벤션을 깨는 최적화는 제안하지 않는다.

## 세션 재개 규칙 (compact 후 동작)

세션이 compact(요약)된 직후 첫 응답에서는 다음을 따른다.

- system-reminder로 주입되는 **과거 skill invocation**(예: `/harness`, `/harness-refine`)과 그에 딸린 ARGUMENTS는 **이미 처리된 이력**으로 간주한다.
- compact 이후 사용자가 새 메시지에서 명시적으로 다시 요청하지 않은 skill은 **자동 실행하지 않는다.**
- compact 직전에 중단됐던 대화 흐름(예: 사용자의 마지막 질문)을 우선 이어간다. 재주입된 skill ARGUMENTS가 현재 맥락과 충돌하면, 실행 전에 사용자에게 "해당 스킬을 다시 실행할지" 한 줄로 확인한다.
- 예외: 사용자가 방금 턴에서 `/<skill-name>`을 직접 입력했거나 명시적으로 "그 스킬 다시 돌려"라고 지시한 경우에만 실행한다.

## 작업 응답 규칙

코드 수정 요청을 받으면 **바로 수정하지 않는다.** 반드시 아래 순서를 따른다.

### 1단계 — 작업 의도 제시 (수정 전 필수)
```
### 작업 의도
- 요청 요약:
- 수정 목적:
- 변경 범위: (수정할 파일 목록)
- 영향 범위: (이 변경으로 영향받는 다른 파일/시스템)
- 리스크/주의점:

### 구현 계획
1. ...
2. ...
3. ...
```

### 2단계 — 사용자 승인 대기
- 작업 의도와 구현 계획을 출력한 후, **사용자의 명시적 승인("진행해줘", "OK" 등)을 받은 후에만** 실제 코드 수정을 진행한다.
- 승인 없이 바로 코드, diff, 패치를 출력하지 않는다.

### 3단계 — 구현 및 코드 설명
코드 수정 완료 후에는 아래 형식으로 코드 설명을 추가한다.

```
### 코드 설명
- **컨테이너/자료구조 선택 이유**: 왜 이 자료구조를 선택했는지 근거
- **복잡한 코드 설명**: 직관적이지 않은 로직의 이유
- **함수 역할 설명**: 각 함수가 무엇을 하는지, 언제 호출되는지
```

### 예외
- 단순 오타 수정, 1줄 변경 등 명백히 안전한 작업은 즉시 수정 가능
- 컴파일 에러 수정 등 긴급 상황은 사후 보고로 대체 가능

## 변경 안전 규칙 (회귀 방지)

코드를 수정할 때 다음 안전 점검을 반드시 수행한다.

### 참조처 검색
- 함수 시그니처, 클래스명, enum 값을 변경할 때는 **모든 호출부/참조처를 grep으로 확인**한다.
- 호출부도 수정이 필요하면 **함께 수정**한다. 호출부 누락은 컴파일 에러 또는 런타임 버그의 원인이다.

### 타입/인터페이스 체크
- `TryGetComponent<T>()`, `GetComponent<T>()` 등 타입 기반 검출 로직을 변경할 때, 해당 타입을 구현하는 **모든 클래스**가 여전히 검출되는지 확인한다.
- 예: `TryGetComponent<Unit>` → Hero는 Unit이 아니므로 검출 불가. IDamageable 등 공통 인터페이스 사용 검토.

### 레이어/물리 체크
- LayerMask, Physics2D 관련 코드를 변경할 때, 영향받는 모든 오브젝트의 레이어 설정을 확인한다.
- 프리팹의 `m_Layer` 값과 코드의 LayerMask가 일치하는지 교차 검증한다.

### UI 좌표계/부모 관계 체크
- UI 오브젝트의 부모를 변경할 때, 앵커/피벗/스케일에 따른 좌표계 영향을 확인한다.
- CanvasScaler, RectTransform의 anchoredPosition 계산이 올바른지 검증한다.

### 직렬화 필드 일관성 체크
- SerializeField 이름이나 타입을 변경할 때, 씬(.unity)과 프리팹(.prefab)의 직렬화 데이터가 코드와 일치하는지 확인한다.
- 씬에서 필드를 변경할 때도 역방향으로 코드의 필드명/타입을 확인한다.
- Unity는 불일치 시 컴파일 에러 없이 조용히 기본값을 사용하므로, 컴파일 통과만으로는 안전하지 않다.

### 컴파일 확인
- 모든 수정 완료 후 Unity 콘솔(`read_console`)로 컴파일 에러가 없는지 확인한다.
- 에러가 있으면 즉시 수정한다.

## 하네스 전달 규칙

하네스(multi-agent)로 작업할 때 다음을 따른다.
하네스 실행은 `/harness` 스킬을 사용한다. Refine은 `/harness-refine` 스킬을 사용한다.

### 4단계 파이프라인
1. **Planner** — 구현 계획 수립 (수정 대상, 순서, 의존 관계). 메인 루프(Fable)가 직접 수행
2. **Critic** — 계획 검증 (과거 실패 학습, 파괴적 변경 탐지, 참조처 매핑). Explore 에이전트(model: fable)
3. **Generator** — Planner 계획 + Critic 검증 결과를 합쳐 받아 코드 생성. general-purpose 에이전트(**model: opus 강제** — 미명시 시 부모 모델을 상속해 토큰 낭비)
4. **Evaluator** — `harness-eval.md` 기준으로 채점. **독립 Explore 에이전트(model: fable)**로 실행하며 입력은 원본 스펙 + git diff + 동작 조건 체크리스트만 전달한다 (Planner/Critic/Generator 산출물 미전달 — 자기 채점 편향 차단). 반증 우선 채점: 스펙 감사 + 적대 시나리오 3개 MCP 실측 의무. 결과 기록(파일 쓰기)은 메인 루프가 수행

### 투명성
- Generator에 전달할 **Planner 계획 + Critic 검증 결과** 합본을 사용자에게 먼저 보여준다.
- 사용자가 AskUserQuestion으로 승인한 후에만 Generator를 실행한다.

### Critic 필수 검증 항목
- 과거 실패 학습 — `.claude/harness-fail-log.md`에서 같은 실수 패턴 확인
- 파괴적 변경 탐지 — 100줄 이상 파일은 Edit만 사용 지시, 수정 범위를 줄 번호로 특정
- 참조처 사전 매핑 — 변경 예정 함수/타입의 모든 호출부를 grep으로 목록화

### Generator 안전 점검 포함
- Generator 프롬프트에는 반드시 "변경 안전 규칙"의 핵심 항목을 포함시킨다:
  1. 참조처 검색 — 수정 대상의 모든 호출부를 grep으로 확인
  2. 호출부 목록 — 발견된 호출부를 코드 설명에 명시
  3. 호출부 포함 수정 — 호출부도 수정이 필요하면 함께 수정
  4. 레이어/인터페이스 체크 — 타입 기반 검출, LayerMask 교차 검증
  5. 직렬화 일관성 — SerializeField 변경 시 씬/프리팹 직렬화 데이터와 코드 필드명/타입 일치 확인
  6. 컴파일 확인 — 수정 후 Unity 콘솔 에러 확인

### 파괴적 변경 감지 (Generator→Evaluator 사이)
- Generator 완료 후 수정된 파일의 줄 수를 비교한다.
- 20% 이상 감소한 파일이 있으면 경고 + git diff 확인 후 의도적 삭제가 아닌 경우 Generator 재실행한다.

### 워크트리 격리 정책 (기본 OFF)
- **기본**: 하네스는 본체 프로젝트에 직접 수정한다 (`isolation: "worktree"` 미사용).
- **이유**: 사용자가 완료 처리 전 Unity에서 바로 플레이테스트할 수 있어 피드백 루프가 빠르다. 파괴 방지는 사전 커밋 스냅샷 + Critic Edit 전용 강제 + 파괴적 변경 감지로 충분히 커버된다.
- **전제 조건**: 하네스 실행 직전 반드시 현재 작업물을 커밋하여 스냅샷을 확정한다. 문제 발생 시 `git reset --hard <pre-harness-commit>`으로 복구.
- **예외 (워크트리 ON 고려 케이스)**:
  - 씬/프리팹이 아닌 코드 파일만 대량(10+) 리팩터하는 작업
  - 멀티 에이전트 병렬 실행
  - Generator가 경로 혼선을 일으킨 전력이 있는 특수 상황
- **씬/프리팹 수정이 포함된 하네스는 반드시 OFF**: Unity Editor가 본체 프로젝트만 바라보므로 워크트리 씬 수정은 반영되지 않아 검증이 불가능하다.

### Refine 재시도 차별화
- 단순히 피드백만 전달하지 않는다.
- 이전 회차에서 **구체적으로 뭘 잘못했고, 이번에는 어떻게 해야 하는지**를 명시한다.
- 동일 파일에 반복 파괴적 변경 시 해당 파일의 수정 범위를 줄 번호로 한정한다.

## 하네스 완료 메시지 규칙

하네스 7단계 완료 메시지에 아래 섹션을 추가한다.

```
### 다음 우선순위
1. {다음 작업}
2. {그 다음 작업}
3. {그 다음 작업}

계속 진행할 항목을 말씀해 주세요.
```

- 다음 우선순위는 이번 구현 내용, `GDD.md`, 기존 코드베이스 맥락을 바탕으로 제안한다.
- "테스트", "문서화" 같은 추상적인 항목은 넣지 않는다.
- 구체적이고 바로 착수 가능한 작업만 나열한다.

### 완료 후 선택지 (AskUserQuestion)
하네스 완료 메시지 출력 직후, AskUserQuestion 도구로 다음 행동 선택지를 반드시 제공한다:
1. **완료 처리** — 결과 확정, 워크트리 사용 시 삭제, 커밋 진행
2. **Refine 진행** — Evaluator 피드백 반영하여 Generator 재실행
3. **결과만 확인** — 코드를 직접 검토 (워크트리 유지)

## 작업 로그 규칙

작업이 완료될 때마다 `Docs/Progress/YYYY-MM-DD.md` 파일에 로그를 기록한다.

### HANDOFF 스냅샷 (세션 인수인계)
- **세션 시작 시 `Docs/Progress/HANDOFF.md`를 먼저 읽는다.** 현재 위치·다음 작업·유효 제약이 여기에 있다.
- 날짜 로그는 append-only 이력, HANDOFF.md는 **항상 덮어쓰는 현재 상태 스냅샷**으로 역할을 분리한다.
- 작업 완료 시점(날짜 로그를 기록할 때)마다 HANDOFF.md도 함께 갱신한다: 현재 위치(커밋/미커밋), 다음 작업(우선순위+차단 관계), 유효 제약, 확정 사양/폐기한 접근.
- 플레이테스트 결과, 대화 중 합의한 결정, 폐기한 접근처럼 **코드가 바뀌지 않은 결정도** HANDOFF.md에 기록한다.

### 규칙
- 파일명은 오늘 날짜(`currentDate` 기준)로 생성한다.
- 같은 날짜 파일이 이미 존재하면 기존 파일에 항목을 추가한다.
- 각 항목에는 작업 시작 시간을 `[HH:MM]` 형식으로 기입한다. 시간을 모를 경우 `[시간 미기록]`으로 표기한다.

### 항목 형식
```
## [HH:MM] 작업 제목

### 완료된 작업
- 파일명 및 변경 내용 요약

### 코드 설명
#### 컨테이너/자료구조 선택 이유
- 왜 이 컨테이너를 선택했는지 근거

#### 함수 설명
- 함수명: 역할, 언제 호출되는지

#### 복잡한 코드 설명
- 직관적이지 않은 로직의 이유

### 다음 단계
- 이어서 할 작업 목록
```
